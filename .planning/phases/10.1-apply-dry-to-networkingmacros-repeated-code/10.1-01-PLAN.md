---
phase: 10.1-apply-dry-to-networkingmacros-repeated-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/HTTPMethodConfig.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Shared/ArgumentExtractors.swift
autonomous: true

must_haves:
  truths:
    - "HTTP macros extract arguments without code duplication"
    - "Per-method configuration (method name, requiresBody, allowsVoidReturn) is centralized"
    - "All extraction helpers compile with zero warnings"
  artifacts:
    - path: "Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/HTTPMethodConfig.swift"
      provides: "HTTPMethodConfig struct with static configurations for GET/POST/PUT/PATCH/DELETE"
      min_lines: 30
    - path: "Packages/NetworkingMacros/Sources/NetworkingMacros/Shared/ArgumentExtractors.swift"
      provides: "ArgumentExtractors enum with extractPath, extractBodyParameter, extractQueryParameters, extractHeaders"
      min_lines: 100
  key_links:
    - from: "HTTPMethodConfig.swift"
      to: "Foundation.Sendable"
      via: "Sendable conformance"
      pattern: "Sendable"
    - from: "ArgumentExtractors.swift"
      to: "SwiftSyntax"
      via: "AttributeSyntax parsing"
      pattern: "AttributeSyntax"
---

<objective>
Create shared infrastructure for HTTP macro DRY refactoring.

Purpose: Establish the foundational types (HTTPMethodConfig, ArgumentExtractors) that all 5 HTTP macros will use. These types extract the "what varies" from the "what is shared" in HTTP macro implementations.

Output: Two new Swift files with shared types that can be imported by HTTP macros.

Note: This phase focuses on HTTP macros only. Configuration macros (Cacheable, Measured, Timeout, DefaultHeaders) are out of scope - they total 524 lines with no significant duplication identified.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-apply-dry-to-networkingmacros-repeated-code/10.1-RESEARCH.md

# HTTP macro reference for extraction patterns
@Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/DELETEMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/Shared/MacroHelpers.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTTPMethodConfig struct</name>
  <files>Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/HTTPMethodConfig.swift</files>
  <action>
Create new file `HTTPMethodConfig.swift` in the HTTP/ directory with:

1. Import statements:
   - `import Foundation` (for Sendable)

2. HTTPMethodConfig struct:
   - Conforms to `Sendable`
   - Properties:
     - `let method: String` (e.g., "GET", "POST")
     - `let requiresBody: Bool` (true for POST/PUT/PATCH, false for GET/DELETE)
     - `let allowsVoidReturn: Bool` (true for DELETE, false for others)
   - Static factory properties:
     - `static let get = HTTPMethodConfig(method: "GET", requiresBody: false, allowsVoidReturn: false)`
     - `static let post = HTTPMethodConfig(method: "POST", requiresBody: true, allowsVoidReturn: false)`
     - `static let put = HTTPMethodConfig(method: "PUT", requiresBody: true, allowsVoidReturn: false)`
     - `static let patch = HTTPMethodConfig(method: "PATCH", requiresBody: true, allowsVoidReturn: false)`
     - `static let delete = HTTPMethodConfig(method: "DELETE", requiresBody: false, allowsVoidReturn: true)`

3. Documentation:
   - File header explaining purpose
   - DocC comments on struct and properties

Keep file under 50 lines total.
  </action>
  <verify>
`swift build --package-path Packages/NetworkingMacros -Xswiftc -warnings-as-errors` succeeds
  </verify>
  <done>
HTTPMethodConfig.swift exists with all 5 static configurations, builds without warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ArgumentExtractors enum</name>
  <files>Packages/NetworkingMacros/Sources/NetworkingMacros/Shared/ArgumentExtractors.swift</files>
  <action>
Create new file `ArgumentExtractors.swift` in the Shared/ directory with:

1. Import statements:
   - `import SwiftSyntax`
   - `import SwiftSyntaxMacros`

2. ArgumentExtractors enum (no cases, static methods only):

   a) `extractPath(from:context:) -> String?`
      - Extract from DELETEMacro lines 134-152
      - Parse first argument from LabeledExprListSyntax
      - Return nil and emit error if not found
      - Method name in error message should be parameterized (use generic "Macro" or accept method name)

   b) `extractBodyParameter(from:context:) -> String?`
      - Extract from POSTMacro lines 186-216
      - Parse `body:` labeled argument
      - Return nil without error (caller handles missing body based on HTTPMethodConfig.requiresBody)

   c) `extractQueryParameters(from:context:) -> [String]`
      - Extract from DELETEMacro lines 155-179
      - Parse `queryParameters:` array argument
      - Return empty array if not found

   d) `extractHeaders(from:context:) -> [String: String]`
      - Extract from DELETEMacro lines 182-216
      - Parse `headers:` dictionary argument
      - Return empty dictionary if not found

3. Documentation:
   - File header explaining these are shared extraction helpers
   - DocC comments on each method

NOTE: Do NOT emit "requires body" error from extractBodyParameter - that decision belongs to the calling macro based on HTTPMethodConfig.requiresBody. Just return nil if not found.
  </action>
  <verify>
`swift build --package-path Packages/NetworkingMacros -Xswiftc -warnings-as-errors` succeeds
  </verify>
  <done>
ArgumentExtractors.swift exists with all 4 extraction methods, builds without warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify infrastructure and run tests</name>
  <files>Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/HTTPMethodConfig.swift, Packages/NetworkingMacros/Sources/NetworkingMacros/Shared/ArgumentExtractors.swift</files>
  <action>
1. Run full test suite:
   - `swift test --package-path Packages/NetworkingMacros`
   - All 33 tests must pass

2. Run MacroTemplateKit tests:
   - `swift test --package-path Packages/MacroTemplateKit`
   - All 110 tests must pass

3. Build all workspace packages:
   - `swift build --package-path Packages/NetworkingMacros -Xswiftc -warnings-as-errors`
   - `swift build --package-path Packages/MacroTemplateKit -Xswiftc -warnings-as-errors`

4. Verify no regressions:
   - Existing HTTP macros still build (they don't use new types yet)
   - New types are available but not yet integrated

If any test fails, fix the issue before completing.
  </action>
  <verify>
`swift test --package-path Packages/NetworkingMacros` shows 33 tests passing
`swift test --package-path Packages/MacroTemplateKit` shows 110 tests passing
  </verify>
  <done>
143 total tests pass (33 + 110), all packages build with warnings-as-errors
  </done>
</task>

</tasks>

<verification>
1. HTTPMethodConfig.swift exists in HTTP/ directory
2. ArgumentExtractors.swift exists in Shared/ directory
3. Both files build without warnings
4. All 143 tests pass
</verification>

<success_criteria>
- HTTPMethodConfig struct exists with 5 static configurations
- ArgumentExtractors enum exists with 4 extraction methods
- All packages build with -Xswiftc -warnings-as-errors
- All 143 tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-apply-dry-to-networkingmacros-repeated-code/10.1-01-SUMMARY.md`
</output>
