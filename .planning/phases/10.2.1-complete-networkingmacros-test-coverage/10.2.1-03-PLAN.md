---
phase: 10.2.1-complete-networkingmacros-test-coverage
plan: 03
type: execute
wave: 2
depends_on: ["10.2.1-01", "10.2.1-02"]
files_modified:
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/AttachedMacroIntegrationTests.swift
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/MacroIntegrationTests.swift
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/IntegrationTests.swift
autonomous: true

must_haves:
  truths:
    - "Integration tests verify multiple macros working together"
    - "@API + @GET + @Headers + @Interceptors combined expansion works"
    - "Full protocol expansion generates complete implementation struct"
  artifacts:
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/AttachedMacroIntegrationTests.swift"
      provides: "Attached macro integration tests"
      contains: "assertMacro"
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/MacroIntegrationTests.swift"
      provides: "General macro integration tests"
      contains: "assertMacro"
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/IntegrationTests.swift"
      provides: "End-to-end integration tests"
      contains: "assertMacro"
  key_links:
    - from: "AttachedMacroIntegrationTests.swift"
      to: "APIMacro, GETMacro, POSTMacro"
      via: "withMacroTesting macros array"
      pattern: "APIMacro\\.self.*GETMacro\\.self"
    - from: "MacroIntegrationTests.swift"
      to: "All macro types"
      via: "withMacroTesting macros array"
      pattern: "withMacroTesting"
---

<objective>
Restore macro integration tests verifying multiple macros working together.

Purpose: Test the complete macro system with @API, HTTP methods, @Headers, @Interceptors, and configuration macros combined.

Output: Three integration test files verifying end-to-end macro expansion behavior.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/GETMacroTests.swift (pattern to follow)
@Packages/NetworkingMacros/Sources/NetworkingMacros/Plugin.swift (all macro registrations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore AttachedMacroIntegrationTests</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/AttachedMacroIntegrationTests.swift</files>
  <action>
Replace the stub file with integration tests for attached macro combinations.

Follow the GETMacroTests.swift pattern:
1. Import MacroTesting and XCTest
2. Import @testable NetworkingMacrosPlugin
3. Override invokeTest() with withMacroTesting including multiple macro types

Test cases to implement:

**@API + HTTP Method combinations:**
- `testAPIWithSingleGET()`: @API protocol with one @GET method generates complete implementation
- `testAPIWithMultipleHTTPMethods()`: @API with @GET, @POST, @DELETE methods
- `testAPIWithPathParameters()`: @API + @GET("/users/{id}") with parameter binding

**@API + Configuration combinations:**
- `testAPIWithDefaultHeaders()`: @API + @DefaultHeaders generates headers property
- `testAPIWithTimeout()`: @API + @Timeout generates timeout property
- `testAPIWithCacheable()`: @API containing @GET @Cacheable method

**Full stack integration:**
- `testFullAPIWithAllFeatures()`: @API + @Interceptors + @DefaultHeaders + @Timeout + @GET + @POST

Note: These tests verify that multiple attached macros coordinate correctly. Use record mode initially to capture actual expansion output, then refine expectations.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter AttachedMacroIntegrationTests 2>&1 | head -50
```
All AttachedMacroIntegrationTests pass.
  </verify>
  <done>AttachedMacroIntegrationTests.swift has 7+ assertMacro tests covering attached macro combinations, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restore MacroIntegrationTests</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/MacroIntegrationTests.swift</files>
  <action>
Replace the stub file with general macro integration tests.

Test cases to implement:

**Macro registration verification:**
- `testAllMacrosRegistered()`: Verify all 13 macros are registered in Plugin.swift

**Cross-macro validation:**
- `testBodyMacroWithPOST()`: @POST with @Body marker validates body parameter
- `testHeadersWithGET()`: @GET with @Headers marker validates header configuration
- `testInterceptorOrderPreserved()`: @Interceptors array order matches generated code

**Error propagation:**
- `testMissingBaseURLPropagates()`: @API error prevents downstream macro expansion
- `testInvalidPathStopsExpansion()`: @GET with invalid path emits error and halts

Note: Focus on how macros interact and validate each other's preconditions.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter MacroIntegrationTests 2>&1 | head -50
```
All MacroIntegrationTests pass.
  </verify>
  <done>MacroIntegrationTests.swift has 6+ assertMacro tests covering macro interactions, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Restore IntegrationTests (end-to-end)</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/IntegrationTests.swift</files>
  <action>
Replace the stub file with end-to-end integration tests.

Test cases to implement:

**Real-world API patterns:**
- `testUserAPIPattern()`: Full UserAPI protocol with CRUD operations
- `testAuthenticatedAPIPattern()`: API with @Interceptors([AuthInterceptor()])
- `testRESTfulResourcePattern()`: Complete REST resource (GET list, GET single, POST, PUT, DELETE)

**Complex scenarios:**
- `testNestedPathParameters()`: /users/{userId}/posts/{postId}/comments/{commentId}
- `testQueryParameterCombinations()`: @GET with multiple query parameters
- `testMixedBodyAndQueryParams()`: @POST with both body and query parameters

Note: These tests validate that the macro system produces production-quality code for realistic API definitions.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter IntegrationTests 2>&1 | head -50
```
All IntegrationTests pass.
  </verify>
  <done>IntegrationTests.swift has 6+ assertMacro tests covering end-to-end scenarios, all tests pass.</done>
</task>

</tasks>

<verification>
```bash
# Verify all three test files have assertMacro calls
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/AttachedMacroIntegrationTests.swift
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/MacroIntegrationTests.swift
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/IntegrationTests.swift

# Verify test count
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test 2>&1 | grep -E "passed|failed"
```
</verification>

<success_criteria>
- AttachedMacroIntegrationTests.swift restored with 7+ assertMacro tests
- MacroIntegrationTests.swift restored with 6+ assertMacro tests
- IntegrationTests.swift restored with 6+ assertMacro tests
- All tests pass in NetworkingMacros package
- Build passes with -Xswiftc -warnings-as-errors
- ~19 new integration tests added
</success_criteria>

<output>
After completion, create `.planning/phases/10.2.1-complete-networkingmacros-test-coverage/10.2.1-03-SUMMARY.md`
</output>
