---
phase: 10.2.1-complete-networkingmacros-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/APIMacroTests.swift
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/BodyMacroTests.swift
autonomous: true

must_haves:
  truths:
    - "@API macro expansion tests verify correct struct generation"
    - "@API macro diagnostic tests verify error cases"
    - "@Body macro expansion tests verify marker behavior"
    - "@Body macro diagnostic tests verify parameter validation"
  artifacts:
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/APIMacroTests.swift"
      provides: "@API macro expansion tests"
      contains: "assertMacro"
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/BodyMacroTests.swift"
      provides: "@Body macro expansion tests"
      contains: "assertMacro"
  key_links:
    - from: "APIMacroTests.swift"
      to: "APIMacro.self"
      via: "withMacroTesting macros parameter"
      pattern: "APIMacro\\.self"
    - from: "BodyMacroTests.swift"
      to: "BodyMacro.self"
      via: "withMacroTesting macros parameter"
      pattern: "BodyMacro\\.self"
---

<objective>
Restore @API and @Body macro expansion tests using MacroTesting framework.

Purpose: Enable test coverage for the two most fundamental macros - @API (protocol-to-client generation) and @Body (request body marker).

Output: Two fully functional test files with assertMacro expansion and diagnostic tests.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/GETMacroTests.swift (pattern to follow)
@Packages/NetworkingMacros/Sources/NetworkingMacros/API/APIMacro.swift (source to test)
@Packages/NetworkingMacros/Sources/NetworkingMacros/BodyMacro.swift (source to test)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore APIMacroTests with MacroTesting framework</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/APIMacroTests.swift</files>
  <action>
Replace the stub file with real expansion tests using MacroTesting framework.

Follow the GETMacroTests.swift pattern exactly:
1. Import MacroTesting and XCTest
2. Import @testable NetworkingMacrosPlugin
3. Override invokeTest() with withMacroTesting(macros: [APIMacro.self])
4. Create test methods with assertMacro {} expansion: {} pattern

Test cases to implement (from original tests at git commit 3bdc72f):

**Expansion tests:**
- `testAPIBasicExpansion()`: @API(baseURL: "https://api.example.com") on empty protocol generates UserAPIImplementation struct with client, baseURL, init
- `testAPIWithComplexProtocolName()`: Protocol name "MyComplexAPIServiceProtocol" generates "MyComplexAPIServiceProtocolImplementation"

**Diagnostic tests:**
- `testAPIRequiresProtocol()`: @API on struct emits "@API can only be applied to protocols"
- `testAPIRequiresBaseURL()`: @API without baseURL emits "@API requires a baseURL argument"
- `testAPIEmptyBaseURL()`: @API(baseURL: "") emits "Base URL cannot be empty"

Note: Use MacroTesting assertMacro syntax (not SwiftSyntaxMacrosTestSupport). The expansion output may differ slightly from original - use record mode first to capture actual output, then adjust expectations.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter APIMacroTests 2>&1 | head -50
```
All APIMacroTests pass.
  </verify>
  <done>APIMacroTests.swift has 5+ assertMacro tests covering @API expansion and diagnostics, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restore BodyMacroTests with MacroTesting framework</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/BodyMacroTests.swift</files>
  <action>
Replace the stub file with real expansion tests using MacroTesting framework.

Follow the GETMacroTests.swift pattern:
1. Import MacroTesting and XCTest
2. Import @testable NetworkingMacrosPlugin
3. Override invokeTest() with withMacroTesting(macros: [BodyMacro.self])

Test cases to implement (from original tests at git commit 3bdc72f):

**Expansion tests (marker macro - returns empty):**
- `testBodyMacroBasicUsage()`: @Body("user") on func createUser(user: User) - no peer code generated
- `testBodyMacroWithMultipleParameters()`: @Body("data") with multiple params validates correctly
- `testBodyMacroWithInternalParameterName()`: @Body("requestBody") with external/internal names

**Diagnostic tests:**
- `testBodyMacroParameterNotFound()`: @Body("nonexistent") emits "Body parameter 'nonexistent' not found"
- `testBodyMacroWithoutArgument()`: @Body without args emits "@Body requires parameter name"
- `testBodyMacroOnNonFunction()`: @Body on struct emits "@Body can only be applied to functions"
- `testBodyMacroMultipleOnSameFunction()`: Two @Body on same func emits "Only one @Body macro allowed"

Note: @Body is a marker macro (PeerMacro) that returns empty array. Expansion tests should show the original function unchanged. Diagnostic tests verify validation errors.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter BodyMacroTests 2>&1 | head -50
```
All BodyMacroTests pass.
  </verify>
  <done>BodyMacroTests.swift has 7+ assertMacro tests covering @Body expansion and diagnostics, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify plan completion and run full test suite</name>
  <files>None</files>
  <action>
Run the full NetworkingMacros test suite to verify all tests pass:

1. Run all tests: `swift test` in NetworkingMacros package
2. Count total tests and assertMacro calls
3. Verify no regressions in existing tests

Expected outcomes:
- APIMacroTests: 5+ tests passing
- BodyMacroTests: 7+ tests passing
- Total NetworkingMacros tests: 79 + 12 = ~91 tests (baseline was 79)
- Zero test failures
- Build passes with warnings-as-errors
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift build -Xswiftc -warnings-as-errors && swift test 2>&1 | tail -30
```
All tests pass, build succeeds with warnings-as-errors.
  </verify>
  <done>Plan 01 complete: APIMacroTests and BodyMacroTests restored with ~12 new tests, full suite passes.</done>
</task>

</tasks>

<verification>
```bash
# Verify both test files have assertMacro calls
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/APIMacroTests.swift
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/BodyMacroTests.swift

# Verify test count increased
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test 2>&1 | grep "Test Suite"
```
</verification>

<success_criteria>
- APIMacroTests.swift restored with 5+ assertMacro tests
- BodyMacroTests.swift restored with 7+ assertMacro tests
- All tests pass in NetworkingMacros package
- Build passes with -Xswiftc -warnings-as-errors
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/10.2.1-complete-networkingmacros-test-coverage/10.2.1-01-SUMMARY.md`
</output>
