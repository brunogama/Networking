---
phase: 10.2.1-complete-networkingmacros-test-coverage
plan: 04
type: execute
wave: 2
depends_on: ["10.2.1-01", "10.2.1-02"]
files_modified:
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestCompositionTests.swift
  - Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestOperatorsTests.swift
autonomous: true

must_haves:
  truths:
    - "Request composition tests verify request building patterns"
    - "Request operators tests verify + operator and merged(with:) patterns"
    - "Tests cover path, query, header, and body composition"
  artifacts:
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestCompositionTests.swift"
      provides: "Request composition pattern tests"
      contains: "assertMacro"
    - path: "Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestOperatorsTests.swift"
      provides: "Request operator tests"
      contains: "assertMacro"
  key_links:
    - from: "RequestCompositionTests.swift"
      to: "HTTP macros"
      via: "withMacroTesting macros array"
      pattern: "GETMacro|POSTMacro"
    - from: "RequestOperatorsTests.swift"
      to: "HTTP macros"
      via: "withMacroTesting macros array"
      pattern: "GETMacro|POSTMacro"
---

<objective>
Restore request composition and operator tests, verifying macro-generated request building patterns.

Purpose: Test that macro-generated code produces correct request construction with path, query, headers, and body composition.

Output: Two test files covering request composition patterns used in macro-generated code.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/GETMacroTests.swift (pattern to follow)
@Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/GETMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/HTTP/POSTMacro.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore RequestCompositionTests</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestCompositionTests.swift</files>
  <action>
Replace the stub file with request composition tests using MacroTesting framework.

Test cases to implement:

**Path composition:**
- `testStaticPathComposition()`: @GET("/users") generates static path
- `testSinglePathParameter()`: @GET("/users/{id}") with id parameter
- `testMultiplePathParameters()`: @GET("/users/{userId}/posts/{postId}") with multiple params
- `testNestedPathComposition()`: Deep path /a/{b}/c/{d}/e/{f}

**Query parameter composition:**
- `testSingleQueryParameter()`: @GET("/users", query: ["page"])
- `testMultipleQueryParameters()`: @GET("/search", query: ["q", "limit", "offset"])
- `testOptionalQueryParameter()`: Query param with Optional type

**Body composition (POST/PUT/PATCH):**
- `testSimpleBodyComposition()`: @POST("/users") with @Body("user")
- `testBodyWithPathParams()`: @POST("/users/{id}") with both path and body

**Header composition:**
- `testStaticHeaderComposition()`: @Headers { H("Accept", "application/json") }
- `testDynamicHeaderComposition()`: @Headers { H("Authorization", "token") } with token param

Note: Focus on how macro-generated code builds HTTPRequest objects with various combinations.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter RequestCompositionTests 2>&1 | head -50
```
All RequestCompositionTests pass.
  </verify>
  <done>RequestCompositionTests.swift has 11+ assertMacro tests covering request composition patterns, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restore RequestOperatorsTests</name>
  <files>Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestOperatorsTests.swift</files>
  <action>
Replace the stub file with request operator tests using MacroTesting framework.

Test cases to implement:

**Basic operator patterns:**
- `testGETRequestStructure()`: @GET generates correct HTTPRequest structure
- `testPOSTRequestStructure()`: @POST generates correct HTTPRequest with body
- `testDELETERequestStructure()`: @DELETE generates correct HTTPRequest

**Response handling patterns:**
- `testDecodeResponsePattern()`: Macro generates JSONDecoder.decode() call
- `testVoidReturnPattern()`: @DELETE returning Void handles no decode
- `testArrayReturnPattern()`: @GET returning [User] decodes array

**Error handling patterns:**
- `testAsyncThrowsSignature()`: Generated methods are async throws
- `testRetryablePattern()`: @GET @Retryable generates retry logic wrapper

**Request modification patterns:**
- `testTimeoutApplication()`: @Timeout(30) applies to generated request
- `testCacheableApplication()`: @Cacheable applies caching configuration

Note: Focus on the operators and patterns used in macro-generated method bodies.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test --filter RequestOperatorsTests 2>&1 | head -50
```
All RequestOperatorsTests pass.
  </verify>
  <done>RequestOperatorsTests.swift has 10+ assertMacro tests covering request operator patterns, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify plan completion and full test suite</name>
  <files>None</files>
  <action>
Run the full NetworkingMacros test suite and verify all tests pass:

1. Run all tests: `swift test` in NetworkingMacros package
2. Count total tests and assertMacro calls
3. Verify no regressions

Expected outcomes:
- RequestCompositionTests: 11+ tests passing
- RequestOperatorsTests: 10+ tests passing
- Combined Plan 04 adds ~21 tests
- Zero test failures
- Build passes with warnings-as-errors
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift build -Xswiftc -warnings-as-errors && swift test 2>&1 | tail -30
```
All tests pass, build succeeds with warnings-as-errors.
  </verify>
  <done>Plan 04 complete: RequestCompositionTests and RequestOperatorsTests restored with ~21 new tests, full suite passes.</done>
</task>

</tasks>

<verification>
```bash
# Verify both test files have assertMacro calls
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestCompositionTests.swift
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/NetworkingMacrosTests/Macros/RequestOperatorsTests.swift

# Count total assertMacro calls
rg "assertMacro" /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros/Tests/ | wc -l
```
</verification>

<success_criteria>
- RequestCompositionTests.swift restored with 11+ assertMacro tests
- RequestOperatorsTests.swift restored with 10+ assertMacro tests
- All tests pass in NetworkingMacros package
- Build passes with -Xswiftc -warnings-as-errors
- ~21 new tests added
</success_criteria>

<output>
After completion, create `.planning/phases/10.2.1-complete-networkingmacros-test-coverage/10.2.1-04-SUMMARY.md`
</output>
