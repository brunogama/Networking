---
phase: 01-swift-6-concurrency-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/Networking/AuthenticationMiddleware.swift
  - Sources/Networking/ProgressTracking.swift
autonomous: true

must_haves:
  truths:
    - "swift build completes without async/await errors"
    - "No 'no async operations occur within await' errors"
  artifacts:
    - path: "Sources/Networking/AuthenticationMiddleware.swift"
      provides: "Fixed async clearRefreshTask call"
    - path: "Sources/Networking/ProgressTracking.swift"
      provides: "Fixed async cleanupStream calls"
  key_links: []
---

<objective>
Fix compilation errors blocking warnings-as-errors builds.

Purpose: Remove 2 compilation errors that prevent `swift build -Xswiftc -warnings-as-errors` from completing. These are blocking all other concurrency work.

Output: Compiling codebase (no errors)
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AuthenticationMiddleware async error</name>
  <files>Sources/Networking/AuthenticationMiddleware.swift</files>
  <action>
Line 164 has `Task { await self.clearRefreshTask() }` but `clearRefreshTask()` is NOT async.

Fix: Remove the `await` keyword. The Task block is already async context, but the inner function is synchronous.

Change from:
```swift
Task { await self.clearRefreshTask() }
```

To:
```swift
Task { self.clearRefreshTask() }
```

If `clearRefreshTask()` needs to be async (has async operations inside), make it async instead.
  </action>
  <verify>swift build 2>&1 | grep -i "AuthenticationMiddleware.*async"</verify>
  <done>No async-related errors in AuthenticationMiddleware.swift</done>
</task>

<task type="auto">
  <name>Task 2: Fix ProgressTracking async errors</name>
  <files>Sources/Networking/ProgressTracking.swift</files>
  <action>
Lines 265 and 275 have `await cleanupStream(transferId)` but `cleanupStream()` is NOT async.

Fix: Remove the `await` keyword from both call sites.

Change from:
```swift
await cleanupStream(transferId)
```

To:
```swift
cleanupStream(transferId)
```

If `cleanupStream()` needs async (e.g., for actor isolation), make it async instead.
  </action>
  <verify>swift build 2>&1 | grep -i "ProgressTracking.*async"</verify>
  <done>No async-related errors in ProgressTracking.swift</done>
</task>

</tasks>

<verification>
```bash
# Verify compilation succeeds
swift build 2>&1 | head -20

# Verify no async errors remain
swift build 2>&1 | grep -c "no 'async' operations"
# Expected: 0
```
</verification>

<success_criteria>
- [ ] `swift build` completes without compilation errors
- [ ] No "no 'async' operations occur within 'await' expression" errors
- [ ] Both files compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-swift-6-concurrency-compliance/01-01-SUMMARY.md`
</output>
