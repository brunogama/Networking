---
phase: 01-swift-6-concurrency-compliance
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "swift build -Xswiftc -warnings-as-errors exits 0"
    - "swift test exits 0"
    - "Zero Thread.sleep in Sources/"
    - "All @unchecked Sendable have documentation"
  artifacts: []
  key_links: []
---

<objective>
Run full verification suite to confirm Phase 1 success criteria.

Purpose: Verify all concurrency requirements (CONC-01 through CONC-10) are satisfied before marking phase complete. This is the gate before Phase 2.

Output: Verification report confirming all criteria pass
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-swift-6-concurrency-compliance/01-06-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build verification</name>
  <files></files>
  <action>
Run build with warnings-as-errors:

```bash
swift build -Xswiftc -warnings-as-errors 2>&1
```

Expected: Exit code 0, no warnings, no errors.

If build fails:
1. Capture error output
2. Identify which plan introduced the issue
3. Fix inline or document for follow-up

Save output to verification report.
  </action>
  <verify>swift build -Xswiftc -warnings-as-errors; echo "Exit code: $?"</verify>
  <done>Build exits 0 with no warnings</done>
</task>

<task type="auto">
  <name>Task 2: Run all tests</name>
  <files></files>
  <action>
Run test suite:

```bash
swift test 2>&1
```

Expected: All tests pass.

If tests fail:
1. Capture failing tests
2. Identify if failure is from concurrency changes
3. Fix or document blockers

Note: Some test failures may be pre-existing. Document which are new vs pre-existing.
  </action>
  <verify>swift test 2>&1 | tail -20</verify>
  <done>All tests pass or failures documented as pre-existing</done>
</task>

<task type="auto">
  <name>Task 3: Run grep verification for requirements</name>
  <files></files>
  <action>
Verify each CONC requirement:

```bash
# CONC-04: Zero Thread.sleep
echo "=== CONC-04: Thread.sleep check ==="
rg "Thread\.sleep" --type swift Sources/
# Expected: No matches

# CONC-05: @unchecked Sendable documented
echo "=== CONC-05: @unchecked Sendable check ==="
rg "@unchecked Sendable" --type swift Sources/ -B 3 | head -50
# Expected: All have /// comments above

# CONC-05: nonisolated(unsafe) documented
echo "=== CONC-05: nonisolated(unsafe) check ==="
rg "nonisolated\(unsafe\)" --type swift Sources/ -B 3
# Expected: All have /// comments above

# CONC-09: Actor reentrancy documented
echo "=== CONC-09: Reentrancy documentation ==="
rg "REENTRANCY" --type swift Sources/
# Expected: Comments in audited actors

# CONC-07: Task lifecycle documented
echo "=== CONC-07: Task lifecycle ==="
rg "LIFECYCLE|cleanupTask|transitionTask" --type swift Sources/
# Expected: Managed tasks or documented fire-and-forget
```

Create summary table of requirement status.
  </action>
  <verify>rg "Thread\.sleep" --type swift Sources/ && echo "FAIL: Thread.sleep found" || echo "PASS: No Thread.sleep"</verify>
  <done>All grep checks pass</done>
</task>

</tasks>

<verification>
Create verification summary:

| Requirement | Check | Status |
|-------------|-------|--------|
| CONC-01 | All public types Sendable | ... |
| CONC-02 | Actor-isolated state | ... |
| CONC-03 | @Sendable closures | ... |
| CONC-04 | Zero Thread.sleep | ... |
| CONC-05 | @unchecked documented | ... |
| CONC-06 | Continuations audited | ... |
| CONC-07 | Task lifecycle managed | ... |
| CONC-08 | Zero warnings | ... |
| CONC-09 | Reentrancy audited | ... |
| CONC-10 | Cancellation checks | ... |
</verification>

<success_criteria>
- [ ] `swift build -Xswiftc -warnings-as-errors` exits 0
- [ ] `swift test` exits 0 (or failures documented as pre-existing)
- [ ] `rg "Thread\.sleep" Sources/` returns empty
- [ ] All `@unchecked Sendable` have documentation
- [ ] All actors have reentrancy audit comments
- [ ] All Task {} instances are managed or documented
- [ ] CONC-01 through CONC-10 verification table complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-swift-6-concurrency-compliance/01-07-SUMMARY.md`

This SUMMARY.md should include the full verification table and serve as the Phase 1 completion report.
</output>
