---
phase: 09-ci-hooks-workspace-automation
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scripts/generate-llms-txt.sh
  - .github/workflows/llms-txt.yml
  - llms.txt
autonomous: true

must_haves:
  truths:
    - "llms.txt generated from public API surface"
    - "All 5 packages included in llms.txt"
    - "Types categorized by kind (struct, protocol, enum, etc.)"
    - "llms.txt auto-updated on source changes"
  artifacts:
    - path: "scripts/generate-llms-txt.sh"
      provides: "LLMs.txt generation script"
      contains: "symbol-graphs"
    - path: ".github/workflows/llms-txt.yml"
      provides: "Automated llms.txt workflow"
      contains: "generate-llms-txt"
    - path: "llms.txt"
      provides: "AI-discoverable API documentation"
      contains: "ModernNetworking"
  key_links:
    - from: ".github/workflows/llms-txt.yml"
      to: "scripts/generate-llms-txt.sh"
      via: "script invocation"
      pattern: "./scripts/generate-llms-txt.sh"
---

<objective>
Implement LLMs.txt generation from Swift symbol graphs for AI-discoverable API documentation across all workspace packages.

Purpose: Create and maintain llms.txt file that exposes the public API surface to AI assistants, enabling better code generation and documentation lookup.

Output:
- `scripts/generate-llms-txt.sh` for symbol extraction and formatting
- `.github/workflows/llms-txt.yml` for automated updates
- Initial `llms.txt` generated from current codebase
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ci-hooks-workspace-automation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create llms.txt generation script</name>
  <files>scripts/generate-llms-txt.sh</files>
  <action>
Create `scripts/generate-llms-txt.sh`:

```bash
#!/bin/bash
# Generate llms.txt from Swift symbol graphs
# Usage: ./scripts/generate-llms-txt.sh

set -e

OUTPUT_FILE="${1:-llms.txt}"
BUILD_DIR=".build/symbol-graphs"

# Define packages in dependency order
packages=(
  "MacroTemplateKit"
  "NetworkingMacros"
  "Networking"
  "NetworkingWebSocket"
  "NetworkingGraphQL"
)

echo "Generating symbol graphs for all packages..."
mkdir -p "$BUILD_DIR"

for pkg in "${packages[@]}"; do
  echo "  Building $pkg..."
  swift build --package-path "Packages/$pkg" \
    -Xswiftc -emit-symbol-graph \
    -Xswiftc -emit-symbol-graph-dir -Xswiftc "$BUILD_DIR/$pkg" 2>/dev/null || true
done

# Start llms.txt output
cat > "$OUTPUT_FILE" << 'HEADER'
# ModernNetworking - Swift HTTP Client Framework

A production-grade Swift networking library featuring async/await, interceptors, macros, and multi-transport support.

## Overview

ModernNetworking is a monorepo containing 5 Swift packages:

| Package | Purpose |
|---------|---------|
| MacroTemplateKit | SwiftSyntax template DSL for macro code generation |
| NetworkingMacros | Swift compiler plugin for HTTP client macros |
| Networking | Core async/await HTTP client with middleware |
| NetworkingWebSocket | Actor-based WebSocket client |
| NetworkingGraphQL | Type-safe GraphQL client |

## Quick Start

```swift
import Networking

// Simple GET request
let client = NetworkClient()
let response = try await client.execute(HTTPRequest(url: URL(string: "https://api.example.com/users")!))

// Using @API macro
@API
protocol UsersAPI {
    @GET("/users")
    func getUsers() async throws -> [User]

    @POST("/users")
    func createUser(@Body user: User) async throws -> User
}
```

## Public API

HEADER

# Extract symbols for each package
for pkg in "${packages[@]}"; do
  SYMBOL_FILE="$BUILD_DIR/$pkg/$pkg.symbols.json"

  if [ ! -f "$SYMBOL_FILE" ]; then
    echo "  Warning: No symbol graph for $pkg"
    continue
  fi

  echo "" >> "$OUTPUT_FILE"
  echo "### $pkg" >> "$OUTPUT_FILE"
  echo "" >> "$OUTPUT_FILE"

  # Extract structs
  structs=$(jq -r '[.symbols[]? | select(.accessLevel == "public" or .accessLevel == "open") | select(.kind.identifier == "swift.struct") | .names.title] | unique | .[]' "$SYMBOL_FILE" 2>/dev/null || true)
  if [ -n "$structs" ]; then
    echo "**Structs:**" >> "$OUTPUT_FILE"
    echo "$structs" | while read -r name; do
      [ -n "$name" ] && echo "- \`$name\`" >> "$OUTPUT_FILE"
    done
    echo "" >> "$OUTPUT_FILE"
  fi

  # Extract protocols
  protocols=$(jq -r '[.symbols[]? | select(.accessLevel == "public" or .accessLevel == "open") | select(.kind.identifier == "swift.protocol") | .names.title] | unique | .[]' "$SYMBOL_FILE" 2>/dev/null || true)
  if [ -n "$protocols" ]; then
    echo "**Protocols:**" >> "$OUTPUT_FILE"
    echo "$protocols" | while read -r name; do
      [ -n "$name" ] && echo "- \`$name\`" >> "$OUTPUT_FILE"
    done
    echo "" >> "$OUTPUT_FILE"
  fi

  # Extract enums
  enums=$(jq -r '[.symbols[]? | select(.accessLevel == "public" or .accessLevel == "open") | select(.kind.identifier == "swift.enum") | .names.title] | unique | .[]' "$SYMBOL_FILE" 2>/dev/null || true)
  if [ -n "$enums" ]; then
    echo "**Enums:**" >> "$OUTPUT_FILE"
    echo "$enums" | while read -r name; do
      [ -n "$name" ] && echo "- \`$name\`" >> "$OUTPUT_FILE"
    done
    echo "" >> "$OUTPUT_FILE"
  fi

  # Extract classes
  classes=$(jq -r '[.symbols[]? | select(.accessLevel == "public" or .accessLevel == "open") | select(.kind.identifier == "swift.class") | .names.title] | unique | .[]' "$SYMBOL_FILE" 2>/dev/null || true)
  if [ -n "$classes" ]; then
    echo "**Classes:**" >> "$OUTPUT_FILE"
    echo "$classes" | while read -r name; do
      [ -n "$name" ] && echo "- \`$name\`" >> "$OUTPUT_FILE"
    done
    echo "" >> "$OUTPUT_FILE"
  fi

  # Extract type aliases
  typealiases=$(jq -r '[.symbols[]? | select(.accessLevel == "public" or .accessLevel == "open") | select(.kind.identifier == "swift.typealias") | .names.title] | unique | .[]' "$SYMBOL_FILE" 2>/dev/null || true)
  if [ -n "$typealiases" ]; then
    echo "**Type Aliases:**" >> "$OUTPUT_FILE"
    echo "$typealiases" | while read -r name; do
      [ -n "$name" ] && echo "- \`$name\`" >> "$OUTPUT_FILE"
    done
    echo "" >> "$OUTPUT_FILE"
  fi
done

# Add footer
cat >> "$OUTPUT_FILE" << 'FOOTER'

## Documentation

- GitHub: https://github.com/brunogama/Networking
- API Reference: https://brunogama.github.io/Networking/documentation/networking

## Requirements

- Swift 6.0+
- iOS 16+, macOS 13+, tvOS 16+, watchOS 9+

## License

MIT License - see LICENSE file for details.

---
*Auto-generated from Swift symbol graphs*
FOOTER

# Count symbols
total=$(grep -c "^- \`" "$OUTPUT_FILE" || echo "0")
echo ""
echo "Generated $OUTPUT_FILE with $total public symbols"
```

Make executable: `chmod +x scripts/generate-llms-txt.sh`
  </action>
  <verify>
- `ls -la scripts/generate-llms-txt.sh` shows executable permissions
- `head -30 scripts/generate-llms-txt.sh` shows header and packages array
- `bash -n scripts/generate-llms-txt.sh && echo "Syntax OK"`
  </verify>
  <done>
generate-llms-txt.sh exists, is executable, extracts symbols from all 5 packages, categorizes by type
  </done>
</task>

<task type="auto">
  <name>Task 2: Create llms.txt workflow</name>
  <files>.github/workflows/llms-txt.yml</files>
  <action>
Create `.github/workflows/llms-txt.yml`:

```yaml
name: Generate LLMs.txt

on:
  push:
    branches: [main, dev]
    paths:
      - 'Packages/**/Sources/**/*.swift'
  workflow_dispatch:

concurrency:
  group: llms-txt-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generate-llms-txt:
    name: Generate LLMs.txt
    runs-on: macos-14
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: '6.0'

      - name: Install jq
        run: brew install jq

      - name: Generate llms.txt
        run: |
          chmod +x scripts/generate-llms-txt.sh
          ./scripts/generate-llms-txt.sh llms.txt

      - name: Show preview
        run: |
          echo "## LLMs.txt Preview" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          head -80 llms.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          total=$(grep -c "^- \`" llms.txt || echo "0")
          echo "**Total public symbols:** $total" >> $GITHUB_STEP_SUMMARY

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add llms.txt
          if git diff --staged --quiet; then
            echo "No changes to llms.txt"
          else
            git commit -m "docs: auto-generate llms.txt from public API"
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: llms-txt
          path: llms.txt
          retention-days: 30
```
  </action>
  <verify>
- `cat .github/workflows/llms-txt.yml | head -20` shows triggers
- `grep "generate-llms-txt.sh" .github/workflows/llms-txt.yml` returns match
- `grep "macos-14" .github/workflows/llms-txt.yml` returns match
  </verify>
  <done>
llms-txt.yml exists with push trigger, Swift setup, script execution, and auto-commit
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate initial llms.txt and commit</name>
  <files>llms.txt, scripts/generate-llms-txt.sh, .github/workflows/llms-txt.yml</files>
  <action>
1. Run the script to generate initial llms.txt:
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking
./scripts/generate-llms-txt.sh llms.txt
```

2. Verify llms.txt content:
```bash
head -50 llms.txt
echo "---"
wc -l llms.txt
grep -c "^- \`" llms.txt || echo "0 symbols"
```

3. Validate workflow YAML:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/llms-txt.yml')); print('llms-txt.yml: OK')"
```

4. Commit all changes:
```bash
git add scripts/generate-llms-txt.sh .github/workflows/llms-txt.yml llms.txt
git commit -m "ci: add llms.txt generation for AI discoverability

- Add scripts/generate-llms-txt.sh for symbol extraction
- Add llms-txt.yml workflow for auto-updates on source changes
- Generate initial llms.txt from all 5 packages
- Categorize symbols by type (struct, protocol, enum, etc.)
- Include quick start examples and documentation links"
```
  </action>
  <verify>
- `cat llms.txt | head -30` shows header and overview
- `grep "### Networking" llms.txt` returns match (package section)
- Git commit succeeds
  </verify>
  <done>
llms.txt generated with public API from all packages, workflow and script committed
  </done>
</task>

</tasks>

<verification>
1. `cat llms.txt | grep "ModernNetworking"` shows title
2. `grep "### MacroTemplateKit" llms.txt` shows package section
3. `grep "### Networking" llms.txt` shows core package section
4. `ls -la scripts/generate-llms-txt.sh` shows executable
5. `cat .github/workflows/llms-txt.yml` shows workflow
6. `git log --oneline -4` shows plan commits 01-04
</verification>

<success_criteria>
- generate-llms-txt.sh extracts symbols from all packages
- llms-txt.yml triggers on source changes
- Initial llms.txt generated with categorized public API
- Quick start example included
- Changes committed locally
</success_criteria>

<output>
After completion, create `.planning/phases/09-ci-hooks-workspace-automation/09-04-SUMMARY.md`
</output>
