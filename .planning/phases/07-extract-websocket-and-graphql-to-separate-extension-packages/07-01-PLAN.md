---
phase: 07-extract-websocket-graphql
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Sources/NetworkingWebSocket/.gitkeep
  - Sources/NetworkingGraphQL/.gitkeep
  - Tests/NetworkingWebSocketTests/.gitkeep
  - Tests/NetworkingGraphQLTests/.gitkeep
autonomous: true

must_haves:
  truths:
    - "Package.swift defines 3 library products (Networking, NetworkingWebSocket, NetworkingGraphQL)"
    - "NetworkingWebSocket target depends only on Networking"
    - "NetworkingGraphQL target depends on Networking and NetworkingMacros"
    - "Core Networking target has NO dependency on extension packages"
    - "Extension test targets exist and are configured"
  artifacts:
    - path: "Package.swift"
      provides: "Multi-product package manifest"
      contains: "library(name: \"NetworkingWebSocket\""
    - path: "Sources/NetworkingWebSocket"
      provides: "WebSocket source directory"
    - path: "Sources/NetworkingGraphQL"
      provides: "GraphQL source directory"
    - path: "Tests/NetworkingWebSocketTests"
      provides: "WebSocket test directory"
    - path: "Tests/NetworkingGraphQLTests"
      provides: "GraphQL test directory"
  key_links:
    - from: "Package.swift"
      to: "NetworkingWebSocket target"
      via: "dependencies: [\"Networking\"]"
      pattern: "target\\(.*name:\\s*\"NetworkingWebSocket\".*dependencies:.*\"Networking\""
    - from: "Package.swift"
      to: "NetworkingGraphQL target"
      via: "dependencies: [\"Networking\", \"NetworkingMacros\"]"
      pattern: "target\\(.*name:\\s*\"NetworkingGraphQL\".*dependencies:.*\"Networking\".*\"NetworkingMacros\""
---

<objective>
Update Package.swift to define multi-product structure with three library products and create the extension package directories.

Purpose: Establish the package manifest foundation for modular architecture before moving files.
Output: Package.swift with 3 library products, empty extension directories ready for file extraction.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-extract-websocket-and-graphql-to-separate-extension-packages/07-RESEARCH.md
@Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extension package directories</name>
  <files>
    Sources/NetworkingWebSocket/.gitkeep
    Sources/NetworkingGraphQL/.gitkeep
    Tests/NetworkingWebSocketTests/.gitkeep
    Tests/NetworkingGraphQLTests/.gitkeep
  </files>
  <action>
    Create the directory structure for both extension packages:

    1. Create Sources/NetworkingWebSocket/ directory
    2. Create Sources/NetworkingGraphQL/ directory
    3. Create Tests/NetworkingWebSocketTests/ directory
    4. Create Tests/NetworkingGraphQLTests/ directory

    Add .gitkeep files to preserve empty directories until files are moved.

    Do NOT move any source files yet - that happens in subsequent plans.
  </action>
  <verify>
    ```bash
    ls -la Sources/NetworkingWebSocket/
    ls -la Sources/NetworkingGraphQL/
    ls -la Tests/NetworkingWebSocketTests/
    ls -la Tests/NetworkingGraphQLTests/
    ```
    All directories exist with .gitkeep files.
  </verify>
  <done>
    Four new directories created: Sources/NetworkingWebSocket/, Sources/NetworkingGraphQL/, Tests/NetworkingWebSocketTests/, Tests/NetworkingGraphQLTests/
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Package.swift with multi-product structure</name>
  <files>Package.swift</files>
  <action>
    Modify Package.swift to define the multi-product structure:

    1. **Add two new library products** to the `products` array:
       ```swift
       .library(name: "NetworkingWebSocket", targets: ["NetworkingWebSocket"]),
       .library(name: "NetworkingGraphQL", targets: ["NetworkingGraphQL"]),
       ```

    2. **Add NetworkingWebSocket target** to targets array:
       ```swift
       .target(
         name: "NetworkingWebSocket",
         dependencies: ["Networking"],
         swiftSettings: [
           .unsafeFlags(["-warn-concurrency", "-enable-actor-data-race-checks"])
         ]
       ),
       ```

    3. **Add NetworkingGraphQL target** to targets array:
       ```swift
       .target(
         name: "NetworkingGraphQL",
         dependencies: [
           "Networking",
           "NetworkingMacros",
         ],
         swiftSettings: [
           .unsafeFlags(["-warn-concurrency", "-enable-actor-data-race-checks"])
         ]
       ),
       ```

    4. **Add NetworkingWebSocketTests test target**:
       ```swift
       .testTarget(
         name: "NetworkingWebSocketTests",
         dependencies: ["NetworkingWebSocket", "Networking"]
       ),
       ```

    5. **Add NetworkingGraphQLTests test target**:
       ```swift
       .testTarget(
         name: "NetworkingGraphQLTests",
         dependencies: [
           "NetworkingGraphQL",
           "Networking",
           "NetworkingMacros",
           .product(name: "MacroTesting", package: "swift-macro-testing"),
         ]
       ),
       ```

    6. **CRITICAL**: Do NOT add any dependency from Networking target to extension packages. The Networking target must remain unchanged (still only depends on NetworkingMacros).

    Preserve all existing configuration (platforms, swift-tools-version, existing dependencies, swiftSettings).
  </action>
  <verify>
    ```bash
    swift package show-dependencies
    swift package describe --type json | grep -E '"name":\s*"Networking(WebSocket|GraphQL)"'
    ```
    Package resolves with no circular dependencies. Three products visible.
  </verify>
  <done>
    Package.swift defines 3 library products with correct one-way dependencies: extensions depend on core, core does NOT depend on extensions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add placeholder source files for extension targets</name>
  <files>
    Sources/NetworkingWebSocket/NetworkingWebSocket.swift
    Sources/NetworkingGraphQL/NetworkingGraphQL.swift
    Tests/NetworkingWebSocketTests/NetworkingWebSocketTests.swift
    Tests/NetworkingGraphQLTests/NetworkingGraphQLTests.swift
  </files>
  <action>
    Create minimal placeholder files so SPM can build the empty targets:

    1. **Sources/NetworkingWebSocket/NetworkingWebSocket.swift**:
       ```swift
       // NetworkingWebSocket module - WebSocket extension for Networking
       // Files will be moved here from Sources/Networking/ in Plan 07-02

       import Foundation
       import Networking

       // Placeholder to allow package compilation
       // This file will be removed when actual files are moved
       enum NetworkingWebSocketPlaceholder {}
       ```

    2. **Sources/NetworkingGraphQL/NetworkingGraphQL.swift**:
       ```swift
       // NetworkingGraphQL module - GraphQL extension for Networking
       // Files will be moved here from Sources/Networking/ in Plan 07-03

       import Foundation
       import Networking

       // Re-export macros for user convenience
       @_exported import NetworkingMacros

       // Placeholder to allow package compilation
       // This file will be removed when actual files are moved
       enum NetworkingGraphQLPlaceholder {}
       ```

    3. **Tests/NetworkingWebSocketTests/NetworkingWebSocketTests.swift**:
       ```swift
       import Testing
       import NetworkingWebSocket

       @Suite("NetworkingWebSocket Placeholder")
       struct NetworkingWebSocketPlaceholderTests {
         @Test("Placeholder test - to be replaced")
         func placeholderTest() {
           // This test will be replaced when actual test files are moved
           #expect(true)
         }
       }
       ```

    4. **Tests/NetworkingGraphQLTests/NetworkingGraphQLTests.swift**:
       ```swift
       import Testing
       import NetworkingGraphQL

       @Suite("NetworkingGraphQL Placeholder")
       struct NetworkingGraphQLPlaceholderTests {
         @Test("Placeholder test - to be replaced")
         func placeholderTest() {
           // This test will be replaced when actual test files are moved
           #expect(true)
         }
       }
       ```

    Remove the .gitkeep files after creating actual source files.
  </action>
  <verify>
    ```bash
    swift build -Xswiftc -warnings-as-errors
    swift test --filter NetworkingWebSocketTests
    swift test --filter NetworkingGraphQLTests
    ```
    Package compiles with warnings-as-errors. Placeholder tests pass.
  </verify>
  <done>
    Extension packages have placeholder files, package compiles, all tests pass.
  </done>
</task>

</tasks>

<verification>
Run verification commands to confirm multi-product package structure:

```bash
# Verify package structure
swift package describe

# Verify no circular dependencies
swift package show-dependencies

# Build all targets with warnings-as-errors
swift build -Xswiftc -warnings-as-errors

# Run all tests
swift test

# Verify Networking target does NOT depend on extensions
swift package show-dependencies | grep -E "Networking.*(WebSocket|GraphQL)"
# Should NOT show core Networking depending on extensions
```
</verification>

<success_criteria>
1. Package.swift defines 3 library products: Networking, NetworkingWebSocket, NetworkingGraphQL
2. NetworkingWebSocket target depends only on Networking (one-way)
3. NetworkingGraphQL target depends on Networking and NetworkingMacros (one-way)
4. Core Networking target has NO dependency on extension packages
5. `swift build -Xswiftc -warnings-as-errors` passes
6. `swift test` passes (including placeholder tests)
7. `swift package show-dependencies` shows acyclic graph
</success_criteria>

<output>
After completion, create `.planning/phases/07-extract-websocket-and-graphql-to-separate-extension-packages/07-01-SUMMARY.md`
</output>
