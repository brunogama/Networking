---
phase: 11-protocol-mocking-for-users
plan: 03
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - Packages/Networking/Sources/Networking/Testing/MockRequestInterceptor.swift
  - Packages/Networking/Sources/Networking/Testing/MockResponseInterceptor.swift
  - Packages/Networking/Sources/Networking/Testing/MockCacheStorage.swift
  - Packages/Networking/Sources/Networking/Testing/MockTimeProvider.swift
autonomous: true

must_haves:
  truths:
    - "User can create MockRequestInterceptor and stub request interception"
    - "User can create MockResponseInterceptor and stub response interception"
    - "User can create MockCacheStorage and stub cache operations"
    - "User can create MockTimeProvider and control time in tests"
  artifacts:
    - path: "Packages/Networking/Sources/Networking/Testing/MockRequestInterceptor.swift"
      provides: "Mock implementation of RequestInterceptor"
      exports: ["MockRequestInterceptor"]
    - path: "Packages/Networking/Sources/Networking/Testing/MockResponseInterceptor.swift"
      provides: "Mock implementation of ResponseInterceptor"
      exports: ["MockResponseInterceptor"]
    - path: "Packages/Networking/Sources/Networking/Testing/MockCacheStorage.swift"
      provides: "Mock implementation of CacheStorage"
      exports: ["MockCacheStorage"]
    - path: "Packages/Networking/Sources/Networking/Testing/MockTimeProvider.swift"
      provides: "Mock implementation of TimeProvider"
      exports: ["MockTimeProvider"]
  key_links:
    - from: "MockRequestInterceptor"
      to: "RequestInterceptor"
      via: "protocol conformance"
      pattern: "MockRequestInterceptor.*RequestInterceptor"
    - from: "MockCacheStorage"
      to: "CacheStorage"
      via: "protocol conformance"
      pattern: "MockCacheStorage.*CacheStorage"
---

<objective>
Create interceptor and infrastructure mock implementations for user testing.

Purpose: Enable framework users to mock interceptors, cache storage, and time provider for comprehensive test coverage.
Output: MockRequestInterceptor.swift, MockResponseInterceptor.swift, MockCacheStorage.swift, MockTimeProvider.swift
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-protocol-mocking-for-users/11-RESEARCH.md
@Packages/Networking/Sources/Networking/Interceptors/InterceptorProtocols.swift
@Packages/Networking/Sources/Networking/CachingMiddleware.swift
@Packages/Networking/Sources/Networking/CircuitBreakerMiddleware.swift
@Packages/Networking/Sources/Networking/Testing/MockVerifiable.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MockRequestInterceptor and MockResponseInterceptor</name>
  <files>
Packages/Networking/Sources/Networking/Testing/MockRequestInterceptor.swift
Packages/Networking/Sources/Networking/Testing/MockResponseInterceptor.swift
  </files>
  <action>
Create MockRequestInterceptor.swift implementing RequestInterceptor and MockVerifiable:

1. Class structure:
   - `public final class MockRequestInterceptor: RequestInterceptor, MockVerifiable, @unchecked Sendable`
   - Private DispatchQueue for thread safety

2. State tracking (queue-protected):
   - `private var interceptHandler: (@Sendable (HTTPRequest, InterceptorChain) async throws -> HTTPResponse)?`
   - `private var interceptCount: Int = 0`
   - `private var capturedRequests: [HTTPRequest] = []`

3. Protocol conformance - RequestInterceptor:
   - `func intercept(request: HTTPRequest, chain: InterceptorChain) async throws -> HTTPResponse`
     - Increment interceptCount, capture request
     - If interceptHandler is set, call it
     - Otherwise, proceed with chain

4. Stubbing methods:
   - `func stubIntercept(_ handler: @escaping @Sendable (HTTPRequest, InterceptorChain) async throws -> HTTPResponse)`
   - `func stubProceed()` - Clears handler, proceeds with chain
   - `func stubShortCircuit(_ response: HTTPResponse)` - Returns response without proceeding

5. MockVerifiable conformance and inspection methods.

---

Create MockResponseInterceptor.swift implementing ResponseInterceptor and MockVerifiable:

1. Same pattern as MockRequestInterceptor but for ResponseInterceptor protocol.

2. Protocol conformance - ResponseInterceptor:
   - `func intercept(response: HTTPResponse, request: HTTPRequest, chain: InterceptorChain) async throws -> HTTPResponse`

3. Stubbing methods:
   - `func stubIntercept(_ handler: @escaping @Sendable (HTTPResponse, HTTPRequest, InterceptorChain) async throws -> HTTPResponse)`
   - `func stubProceed()` - Clears handler, proceeds with chain
   - `func stubReplace(_ response: HTTPResponse)` - Returns different response

Note: Check InterceptorProtocols.swift for exact protocol signatures and adapt accordingly.
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` passes
`rg "class MockRequestInterceptor.*RequestInterceptor" Packages/Networking/Sources/Networking/Testing/` returns match
`rg "class MockResponseInterceptor.*ResponseInterceptor" Packages/Networking/Sources/Networking/Testing/` returns match
  </verify>
  <done>
MockRequestInterceptor exists with stubIntercept, stubProceed, stubShortCircuit methods.
MockResponseInterceptor exists with stubIntercept, stubProceed, stubReplace methods.
Both implement their protocol and MockVerifiable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MockCacheStorage</name>
  <files>Packages/Networking/Sources/Networking/Testing/MockCacheStorage.swift</files>
  <action>
Create MockCacheStorage.swift implementing CachingMiddleware.CacheStorage and MockVerifiable.

First, check CachingMiddleware.swift for CacheStorage protocol definition (nested inside CachingMiddleware).

1. Class structure (use actor for thread safety, not DispatchQueue - per research recommendation):
   - `public actor MockCacheStorage: CachingMiddleware.CacheStorage, MockVerifiable`
   - No @unchecked Sendable needed - actors are Sendable by design

2. State tracking:
   - `private var storage: [String: CachingMiddleware.CacheEntry] = [:]`
   - `private var getCalls: [String] = []`
   - `private var setCalls: [(key: String, entry: CachingMiddleware.CacheEntry)] = []`
   - `private var removeCalls: [String] = []`

3. Protocol conformance - CacheStorage:
   - `func get(_ key: String) async -> CachingMiddleware.CacheEntry?`
   - `func set(_ key: String, entry: CachingMiddleware.CacheEntry) async`
   - `func remove(_ key: String) async`
   - `func clear() async`

4. Stubbing methods:
   - `func stub(key: String, entry: CachingMiddleware.CacheEntry)`
   - `func stubEmpty()` - Clears storage

5. MockVerifiable conformance:
   - `public nonisolated var callCount: Int` - Returns sum of all operation counts (via async access)

   Note: Since actors isolate state, callCount needs special handling. Use `nonisolated` with internal async accessor or return a synchronous snapshot.

6. Verification methods:
   - `func verifyGet(_ key: String, times: Int = 1) async throws`
   - `func verifySet(_ key: String, times: Int = 1) async throws`
   - `func verifyRemove(_ key: String, times: Int = 1) async throws`

7. Inspection:
   - `func getStoredKeys() -> [String]`
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` passes
`rg "actor MockCacheStorage.*CacheStorage" Packages/Networking/Sources/Networking/Testing/` returns match
  </verify>
  <done>
MockCacheStorage exists as actor with stub, verifyGet, verifySet, verifyRemove methods.
Implements CachingMiddleware.CacheStorage and MockVerifiable protocols.
Uses actor isolation for thread safety.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create MockTimeProvider</name>
  <files>Packages/Networking/Sources/Networking/Testing/MockTimeProvider.swift</files>
  <action>
Create MockTimeProvider.swift implementing TimeProvider and MockVerifiable:

1. Class structure:
   - `public final class MockTimeProvider: TimeProvider, MockVerifiable, @unchecked Sendable`
   - Private DispatchQueue for thread safety

2. State tracking (queue-protected):
   - `private var currentTime: Date`
   - `private var nowCallCount: Int = 0`

3. Protocol conformance - TimeProvider:
   - `func now() -> Date`
     - Increment nowCallCount
     - Return currentTime

4. Initialization:
   - `public init(initialTime: Date = Date())` - Sets starting time
   - `public convenience init(timestamp: TimeInterval)` - Create from Unix timestamp

5. Time manipulation methods:
   - `func setTime(_ date: Date)` - Sets current time
   - `func advance(by interval: TimeInterval)` - Advances time forward
   - `func rewind(by interval: TimeInterval)` - Moves time backward
   - `func reset()` - Returns to initial time

6. MockVerifiable conformance:
   - `public var callCount: Int` - Returns nowCallCount

7. Convenience methods:
   - `static func frozen(at date: Date) -> MockTimeProvider` - Creates provider fixed at specific time

8. Add @unchecked Sendable justification comment.
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` passes
`rg "class MockTimeProvider.*TimeProvider" Packages/Networking/Sources/Networking/Testing/` returns match
  </verify>
  <done>
MockTimeProvider exists with setTime, advance, rewind, reset methods.
Implements TimeProvider and MockVerifiable protocols.
Can control time for circuit breaker and timeout testing.
  </done>
</task>

</tasks>

<verification>
1. All 4 files exist in Packages/Networking/Sources/Networking/Testing/
2. `swift build -Xswiftc -warnings-as-errors` passes for entire workspace
3. No SwiftLint violations in new files
4. All mocks implement MockVerifiable protocol
5. MockCacheStorage uses actor isolation (no @unchecked Sendable)
6. Other mocks use DispatchQueue with @unchecked Sendable and justification
</verification>

<success_criteria>
1. MockRequestInterceptor and MockResponseInterceptor can stub interceptor chain behavior
2. MockCacheStorage can stub and verify cache operations
3. MockTimeProvider allows controlling time in tests
4. All mocks are Swift 6 concurrency compliant
5. Build passes with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/11-protocol-mocking-for-users/11-03-SUMMARY.md`
</output>
