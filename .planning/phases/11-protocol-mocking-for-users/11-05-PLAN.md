---
phase: 11-protocol-mocking-for-users
plan: 05
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - Packages/Networking/Tests/NetworkingTests/MocksTests/MockVerifiableTests.swift
  - Packages/Networking/Tests/NetworkingTests/MocksTests/MockAuthProviderTests.swift
  - Packages/Networking/Tests/NetworkingTests/MocksTests/MockMiddlewareTests.swift
  - Packages/Networking/Tests/NetworkingTests/MocksTests/MockInfrastructureTests.swift
  - Packages/Networking/Sources/Networking/Documentation.docc/Articles/TESTING_GUIDE.md
autonomous: true

must_haves:
  truths:
    - "All new mocks have comprehensive test coverage"
    - "Documentation explains how users mock protocols in their tests"
    - "Tests verify Swift 6 concurrency compliance"
  artifacts:
    - path: "Packages/Networking/Tests/NetworkingTests/MocksTests/MockVerifiableTests.swift"
      provides: "Tests for MockVerifiable protocol"
      min_lines: 30
    - path: "Packages/Networking/Tests/NetworkingTests/MocksTests/MockAuthProviderTests.swift"
      provides: "Tests for auth provider mocks"
      min_lines: 50
    - path: "Packages/Networking/Tests/NetworkingTests/MocksTests/MockMiddlewareTests.swift"
      provides: "Tests for middleware mocks"
      min_lines: 50
    - path: "Packages/Networking/Tests/NetworkingTests/MocksTests/MockInfrastructureTests.swift"
      provides: "Tests for infrastructure mocks"
      min_lines: 50
  key_links:
    - from: "MockAuthProviderTests"
      to: "MockBearerTokenProvider"
      via: "test coverage"
      pattern: "MockBearerTokenProvider"
    - from: "TESTING_GUIDE.md"
      to: "Mocks.swift"
      via: "documentation reference"
      pattern: "MockHTTPClient|MockBearerTokenProvider"
---

<objective>
Create comprehensive tests and documentation for all protocol mocks.

Purpose: Ensure all mocks work correctly and users have clear documentation for mocking protocols in their tests.
Output: Test files for all mocks, updated TESTING_GUIDE.md with protocol mocking section
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-protocol-mocking-for-users/11-RESEARCH.md
@Packages/Networking/Sources/Networking/Testing/MockVerifiable.swift
@Packages/Networking/Sources/Networking/Testing/MockBearerTokenProvider.swift
@Packages/Networking/Sources/Networking/Documentation.docc/Articles/TESTING_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MockVerifiable and MockAuthProvider tests</name>
  <files>
Packages/Networking/Tests/NetworkingTests/MocksTests/MockVerifiableTests.swift
Packages/Networking/Tests/NetworkingTests/MocksTests/MockAuthProviderTests.swift
  </files>
  <action>
Create MocksTests directory and add test files.

**MockVerifiableTests.swift:**

1. Test MockVerifiable protocol default implementations:
   - `test_verifyCalledOnce_succeeds_whenCalledOnce()`
   - `test_verifyCalledOnce_throws_whenCalledTwice()`
   - `test_verifyCalledOnce_throws_whenNeverCalled()`
   - `test_verifyCalledExactly_succeeds_whenCountMatches()`
   - `test_verifyNeverCalled_succeeds_whenNeverCalled()`
   - `test_verifyCalledAtLeast_succeeds_whenCalledEnough()`

2. Test MockError enum:
   - `test_mockError_notStubbed_hasDescriptiveMessage()`
   - `test_mockError_unexpectedCallCount_showsExpectedAndActual()`

**MockAuthProviderTests.swift:**

1. Test MockBearerTokenProvider:
   - `test_getCurrentToken_returnsStubbed_whenStubSet()`
   - `test_getCurrentToken_throws_whenNoStub()`
   - `test_refreshToken_returnsStubbed_whenStubSet()`
   - `test_verifyTokenFetched_succeeds_afterTokenFetch()`
   - `test_verifyRefreshed_succeeds_afterRefresh()`
   - `test_callCount_incrementsOnEachCall()`

2. Test MockCustomAuthProvider:
   - `test_authenticateRequest_appliesTransform_whenStubbed()`
   - `test_authenticateRequest_returnsUnchanged_whenNoStub()`
   - `test_handleAuthenticationError_callsHandler_whenStubbed()`
   - `test_capturedRequests_recordsAllRequests()`

Use `@Test` from Swift Testing framework.
Use `#expect()` for assertions.
All tests must be `async throws` where needed.
  </action>
  <verify>
`mkdir -p Packages/Networking/Tests/NetworkingTests/MocksTests && swift test --filter MockVerifiableTests` passes
`swift test --filter MockAuthProviderTests` passes
  </verify>
  <done>
MockVerifiableTests.swift has 8+ tests covering all MockVerifiable methods.
MockAuthProviderTests.swift has 10+ tests covering BearerTokenProvider and CustomAuthProvider mocks.
All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MockMiddleware and MockInfrastructure tests</name>
  <files>
Packages/Networking/Tests/NetworkingTests/MocksTests/MockMiddlewareTests.swift
Packages/Networking/Tests/NetworkingTests/MocksTests/MockInfrastructureTests.swift
  </files>
  <action>
**MockMiddlewareTests.swift:**

1. Test MockHTTPRequestMiddleware:
   - `test_process_appliesTransform_whenStubbed()`
   - `test_process_passesThrough_whenNoStub()`
   - `test_process_throws_whenStubFailure()`
   - `test_stubAddHeader_addsHeaderToRequest()`
   - `test_capturedRequests_recordsAllProcessedRequests()`

2. Test MockHTTPResponseMiddleware:
   - `test_process_appliesTransform_whenStubbed()`
   - `test_process_passesThrough_whenNoStub()`
   - `test_stubReplaceBody_replacesResponseBody()`
   - `test_capturedResponses_recordsAllProcessedResponses()`

3. Test MockHTTPErrorMiddleware:
   - `test_handle_callsHandler_whenStubbed()`
   - `test_handle_rethrows_whenStubRethrow()`
   - `test_stubRecoveryResponse_returnsResponse()`
   - `test_capturedErrors_recordsAllHandledErrors()`

**MockInfrastructureTests.swift:**

1. Test MockCacheStorage:
   - `test_get_returnsStubbed_whenKeyExists()`
   - `test_get_returnsNil_whenKeyMissing()`
   - `test_set_storesEntry()`
   - `test_remove_deletesEntry()`
   - `test_verifyGet_succeeds_afterGet()`
   - `test_verifySet_succeeds_afterSet()`

2. Test MockTimeProvider:
   - `test_now_returnsStubbedTime()`
   - `test_advance_movesTimeForward()`
   - `test_rewind_movesTimeBackward()`
   - `test_frozen_returnsConstantTime()`

3. Test MockMetricsCollector (if applicable):
   - `test_record_capturesEvents()`
   - `test_verifyEventRecorded_succeeds_afterRecord()`

4. Test MockTraceExporter (if applicable):
   - `test_export_capturesSpans()`
   - `test_verifySpanExported_succeeds_afterExport()`

All tests must use `async` for actor-based mocks.
  </action>
  <verify>
`swift test --filter MockMiddlewareTests` passes
`swift test --filter MockInfrastructureTests` passes
  </verify>
  <done>
MockMiddlewareTests.swift has 12+ tests covering all middleware mocks.
MockInfrastructureTests.swift has 12+ tests covering cache, time, metrics, trace mocks.
All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update TESTING_GUIDE.md with protocol mocking section</name>
  <files>Packages/Networking/Sources/Networking/Documentation.docc/Articles/TESTING_GUIDE.md</files>
  <action>
Add a new section to TESTING_GUIDE.md (or create if missing) documenting protocol mocking.

**Section: Protocol Mocking for User Tests**

1. Introduction:
   - Explain that framework provides mock implementations for all major protocols
   - All mocks conform to MockVerifiable for consistent verification

2. Available Mocks (with brief description):
   - MockHTTPClient (alias for MockNetworkClient)
   - MockBearerTokenProvider
   - MockCustomAuthProvider
   - MockHTTPRequestMiddleware
   - MockHTTPResponseMiddleware
   - MockHTTPErrorMiddleware
   - MockRequestInterceptor
   - MockResponseInterceptor
   - MockCacheStorage
   - MockTimeProvider
   - MockMetricsCollector
   - MockTraceExporter

3. Usage Examples:

   **Example 1: Testing with Mock Token Provider**
   ```swift
   @Test
   func userService_fetchesProfile_withValidToken() async throws {
     let mockTokenProvider = MockBearerTokenProvider()
     mockTokenProvider.stubToken("test-token-123")

     let client = NetworkClient {
       BaseURL("https://api.example.com")
       BearerAuth(provider: mockTokenProvider)
     }

     let request = HTTPRequest { GET("/profile") }
     let response = try await client.execute(request)

     #expect(response.status == .ok)
     try mockTokenProvider.verifyCalledOnce()
   }
   ```

   **Example 2: Testing Custom Middleware**
   ```swift
   @Test
   func middleware_addsCustomHeader() async throws {
     let mockMiddleware = MockHTTPRequestMiddleware()
     mockMiddleware.stubAddHeader("X-Custom", value: "test-value")

     let request = HTTPRequest { GET("/users") }
     let processed = try await mockMiddleware.process(request)

     #expect(processed.headers["X-Custom"] == "test-value")
     try mockMiddleware.verifyCalledOnce()
   }
   ```

   **Example 3: Testing Cache Behavior**
   ```swift
   @Test
   func caching_storesResponse() async throws {
     let mockCache = MockCacheStorage()

     // ... configure client with mock cache ...

     _ = try await client.execute(request)

     try await mockCache.verifySet("cache-key", times: 1)
   }
   ```

4. Verification Patterns:
   - Explain MockVerifiable methods
   - Show verifyCalledOnce, verifyCalledExactly, verifyNeverCalled usage

5. Best Practices:
   - Use actors for state-heavy mocks (MockCacheStorage, MockMetricsCollector)
   - Use DispatchQueue for simple mocks
   - Always verify expectations to catch missing calls
   - Reset mocks between tests if reusing

Check if TESTING_GUIDE.md exists. If not, check for similar documentation file and add section there.
If no testing guide exists, create minimal TESTING_GUIDE.md with this content.
  </action>
  <verify>
File exists: `ls Packages/Networking/Sources/Networking/Documentation.docc/Articles/TESTING_GUIDE.md`
Contains section: `rg "Protocol Mocking" Packages/Networking/Sources/Networking/Documentation.docc/` returns match
  </verify>
  <done>
TESTING_GUIDE.md contains "Protocol Mocking for User Tests" section.
Section includes all 12 mock types and usage examples.
Best practices documented for mock verification.
  </done>
</task>

</tasks>

<verification>
1. All test files exist in Packages/Networking/Tests/NetworkingTests/MocksTests/
2. `swift test --filter MocksTests` passes (all mock tests pass)
3. TESTING_GUIDE.md contains protocol mocking documentation
4. Documentation includes all 12 mock types with examples
5. `swift build -Xswiftc -warnings-as-errors` passes for entire workspace
</verification>

<success_criteria>
1. 40+ tests cover all mock implementations
2. All tests pass with `swift test`
3. Documentation explains protocol mocking with clear examples
4. Users can follow guide to mock any supported protocol
5. Build passes with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/11-protocol-mocking-for-users/11-05-SUMMARY.md`
</output>
