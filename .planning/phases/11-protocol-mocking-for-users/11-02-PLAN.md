---
phase: 11-protocol-mocking-for-users
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - Packages/Networking/Sources/Networking/Testing/MockHTTPRequestMiddleware.swift
  - Packages/Networking/Sources/Networking/Testing/MockHTTPResponseMiddleware.swift
  - Packages/Networking/Sources/Networking/Testing/MockHTTPErrorMiddleware.swift
autonomous: true

must_haves:
  truths:
    - "User can create MockHTTPRequestMiddleware and stub request transformations"
    - "User can create MockHTTPResponseMiddleware and stub response transformations"
    - "User can create MockHTTPErrorMiddleware and stub error handling"
    - "All middleware mocks verify call counts"
  artifacts:
    - path: "Packages/Networking/Sources/Networking/Testing/MockHTTPRequestMiddleware.swift"
      provides: "Mock implementation of HTTPRequestMiddleware"
      exports: ["MockHTTPRequestMiddleware"]
    - path: "Packages/Networking/Sources/Networking/Testing/MockHTTPResponseMiddleware.swift"
      provides: "Mock implementation of HTTPResponseMiddleware"
      exports: ["MockHTTPResponseMiddleware"]
    - path: "Packages/Networking/Sources/Networking/Testing/MockHTTPErrorMiddleware.swift"
      provides: "Mock implementation of HTTPErrorMiddleware"
      exports: ["MockHTTPErrorMiddleware"]
  key_links:
    - from: "MockHTTPRequestMiddleware"
      to: "HTTPRequestMiddleware"
      via: "protocol conformance"
      pattern: "MockHTTPRequestMiddleware.*HTTPRequestMiddleware"
    - from: "MockHTTPResponseMiddleware"
      to: "HTTPResponseMiddleware"
      via: "protocol conformance"
      pattern: "MockHTTPResponseMiddleware.*HTTPResponseMiddleware"
    - from: "MockHTTPErrorMiddleware"
      to: "HTTPErrorMiddleware"
      via: "protocol conformance"
      pattern: "MockHTTPErrorMiddleware.*HTTPErrorMiddleware"
---

<objective>
Create middleware mock implementations for user testing.

Purpose: Enable framework users to mock HTTP middleware (request, response, error) in their tests for verifying middleware chain behavior.
Output: MockHTTPRequestMiddleware.swift, MockHTTPResponseMiddleware.swift, MockHTTPErrorMiddleware.swift
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-protocol-mocking-for-users/11-RESEARCH.md
@Packages/Networking/Sources/Networking/HTTPClient.swift
@Packages/Networking/Sources/Networking/Testing/MockVerifiable.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MockHTTPRequestMiddleware</name>
  <files>Packages/Networking/Sources/Networking/Testing/MockHTTPRequestMiddleware.swift</files>
  <action>
Create MockHTTPRequestMiddleware.swift implementing HTTPRequestMiddleware and MockVerifiable:

1. Class structure:
   - `public final class MockHTTPRequestMiddleware: HTTPRequestMiddleware, MockVerifiable, @unchecked Sendable`
   - Private DispatchQueue for thread safety

2. State tracking (queue-protected):
   - `private var transform: (@Sendable (HTTPRequest) async throws -> HTTPRequest)?`
   - `private var processCount: Int = 0`
   - `private var capturedRequests: [HTTPRequest] = []`
   - `private var shouldFail: Error?`

3. Protocol conformance - HTTPRequestMiddleware:
   - `func process(_ request: HTTPRequest) async throws -> HTTPRequest`
     - If shouldFail is set, throw it
     - If transform is set, apply it
     - Otherwise return request unchanged
     - Always: increment processCount, capture request

4. Stubbing methods:
   - `func stubTransform(_ transform: @escaping @Sendable (HTTPRequest) async throws -> HTTPRequest)`
   - `func stubPassthrough()` - Clears transform (request passes through unchanged)
   - `func stubFailure(_ error: Error)` - Sets shouldFail
   - `func stubAddHeader(_ name: String, value: String)` - Convenience for adding header

5. MockVerifiable conformance:
   - `public var callCount: Int` - Returns processCount

6. Inspection:
   - `func getCapturedRequests() -> [HTTPRequest]`
   - `func getLastCapturedRequest() -> HTTPRequest?`

7. Reset:
   - `func reset()` - Clears all state

8. Add @unchecked Sendable justification comment.
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` passes
`rg "class MockHTTPRequestMiddleware.*HTTPRequestMiddleware" Packages/Networking/Sources/Networking/Testing/` returns match
  </verify>
  <done>
MockHTTPRequestMiddleware exists with stubTransform, stubPassthrough, stubFailure, stubAddHeader methods.
Implements HTTPRequestMiddleware and MockVerifiable protocols.
Can capture and inspect processed requests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MockHTTPResponseMiddleware</name>
  <files>Packages/Networking/Sources/Networking/Testing/MockHTTPResponseMiddleware.swift</files>
  <action>
Create MockHTTPResponseMiddleware.swift implementing HTTPResponseMiddleware and MockVerifiable:

1. Class structure:
   - `public final class MockHTTPResponseMiddleware: HTTPResponseMiddleware, MockVerifiable, @unchecked Sendable`
   - Private DispatchQueue for thread safety

2. State tracking (queue-protected):
   - `private var transform: (@Sendable (HTTPResponse, HTTPRequest) async throws -> HTTPResponse)?`
   - `private var processCount: Int = 0`
   - `private var capturedResponses: [(request: HTTPRequest, response: HTTPResponse)] = []`
   - `private var shouldFail: Error?`

3. Protocol conformance - HTTPResponseMiddleware:
   - `func process(_ response: HTTPResponse, for request: HTTPRequest) async throws -> HTTPResponse`
     - If shouldFail is set, throw it
     - If transform is set, apply it
     - Otherwise return response unchanged
     - Always: increment processCount, capture (request, response) pair

4. Stubbing methods:
   - `func stubTransform(_ transform: @escaping @Sendable (HTTPResponse, HTTPRequest) async throws -> HTTPResponse)`
   - `func stubPassthrough()` - Clears transform
   - `func stubFailure(_ error: Error)` - Sets shouldFail
   - `func stubReplaceBody(_ body: Data)` - Convenience for replacing response body

5. MockVerifiable conformance:
   - `public var callCount: Int` - Returns processCount

6. Inspection:
   - `func getCapturedResponses() -> [(request: HTTPRequest, response: HTTPResponse)]`
   - `func getLastCapturedResponse() -> (request: HTTPRequest, response: HTTPResponse)?`

7. Reset:
   - `func reset()` - Clears all state

8. Add @unchecked Sendable justification comment.
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` passes
`rg "class MockHTTPResponseMiddleware.*HTTPResponseMiddleware" Packages/Networking/Sources/Networking/Testing/` returns match
  </verify>
  <done>
MockHTTPResponseMiddleware exists with stubTransform, stubPassthrough, stubFailure, stubReplaceBody methods.
Implements HTTPResponseMiddleware and MockVerifiable protocols.
Can capture and inspect processed responses.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create MockHTTPErrorMiddleware</name>
  <files>Packages/Networking/Sources/Networking/Testing/MockHTTPErrorMiddleware.swift</files>
  <action>
Create MockHTTPErrorMiddleware.swift implementing HTTPErrorMiddleware and MockVerifiable:

1. Class structure:
   - `public final class MockHTTPErrorMiddleware: HTTPErrorMiddleware, MockVerifiable, @unchecked Sendable`
   - Private DispatchQueue for thread safety

2. State tracking (queue-protected):
   - `private var handler: (@Sendable (Error, HTTPRequest) async throws -> HTTPResponse)?`
   - `private var handleCount: Int = 0`
   - `private var capturedErrors: [(error: Error, request: HTTPRequest)] = []`
   - `private var shouldRethrow: Bool = true`

3. Protocol conformance - HTTPErrorMiddleware:
   - `func handle(_ error: Error, for request: HTTPRequest) async throws -> HTTPResponse`
     - Increment handleCount, capture (error, request) pair
     - If handler is set, call it and return result
     - If shouldRethrow is true, rethrow the error
     - Otherwise, return a default error response

4. Stubbing methods:
   - `func stubHandler(_ handler: @escaping @Sendable (Error, HTTPRequest) async throws -> HTTPResponse)`
   - `func stubRethrow()` - Sets shouldRethrow = true (default behavior)
   - `func stubSwallow()` - Sets shouldRethrow = false, returns default response
   - `func stubRecoveryResponse(_ response: HTTPResponse)` - Convenience for error recovery

5. MockVerifiable conformance:
   - `public var callCount: Int` - Returns handleCount

6. Inspection:
   - `func getCapturedErrors() -> [(error: Error, request: HTTPRequest)]`
   - `func getLastCapturedError() -> (error: Error, request: HTTPRequest)?`

7. Reset:
   - `func reset()` - Clears all state

8. Add @unchecked Sendable justification comment.
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` passes
`rg "class MockHTTPErrorMiddleware.*HTTPErrorMiddleware" Packages/Networking/Sources/Networking/Testing/` returns match
  </verify>
  <done>
MockHTTPErrorMiddleware exists with stubHandler, stubRethrow, stubSwallow, stubRecoveryResponse methods.
Implements HTTPErrorMiddleware and MockVerifiable protocols.
Can capture and inspect handled errors.
  </done>
</task>

</tasks>

<verification>
1. All 3 files exist in Packages/Networking/Sources/Networking/Testing/
2. `swift build -Xswiftc -warnings-as-errors` passes for entire workspace
3. No SwiftLint violations in new files
4. All middleware mocks implement MockVerifiable protocol
5. All @unchecked Sendable usages have justification comments
</verification>

<success_criteria>
1. MockHTTPRequestMiddleware can stub and capture request transformations
2. MockHTTPResponseMiddleware can stub and capture response transformations
3. MockHTTPErrorMiddleware can stub error handling and recovery
4. All mocks are Swift 6 concurrency compliant
5. Build passes with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/11-protocol-mocking-for-users/11-02-SUMMARY.md`
</output>
