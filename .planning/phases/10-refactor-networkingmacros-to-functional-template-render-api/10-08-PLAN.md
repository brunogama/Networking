---
phase: 10-refactor-networkingmacros-to-functional-template-render-api
plan: 08
type: execute
wave: 8
depends_on: ["10-07"]
files_modified:
  - Packages/NetworkingMacros/Sources/NetworkingMacros/API/APIMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/BodyMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/HeadersMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Interceptors/InterceptorsMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Interceptors/InterceptorCodeGenerator.swift
autonomous: true

must_haves:
  truths:
    - "All remaining macros use Template algebra instead of raw SwiftSyntax"
    - "Zero raw SwiftSyntax node construction in macro expansion methods"
    - "All macro tests pass with identical expansion output"
    - "Phase 10 complete with all 13 macros refactored"
  artifacts:
    - path: "Packages/NetworkingMacros/Sources/NetworkingMacros/API/APIMacro.swift"
      provides: "Refactored @API macro using Template algebra"
      contains: "import MacroTemplateKit"
    - path: "Packages/NetworkingMacros/Sources/NetworkingMacros/Interceptors/InterceptorCodeGenerator.swift"
      provides: "Refactored interceptor code generator using Template algebra"
      contains: "import MacroTemplateKit"
  key_links:
    - from: "API/APIMacro.swift"
      to: "MacroTemplateKit"
      via: "Template construction for API class generation"
      pattern: "Template<"
---

<objective>
Refactor remaining macros (API, Body, Headers, Interceptors) to use MacroTemplateKit's Template algebra, completing Phase 10.

Purpose: Complete the macro refactoring to eliminate all raw SwiftSyntax construction.
Output: 4 refactored macro files, all 13 macros now using Template algebra.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-refactor-networkingmacros-to-functional-template-render-api/10-07-SUMMARY.md

# Current macro implementations to refactor
@Packages/NetworkingMacros/Sources/NetworkingMacros/API/APIMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/BodyMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/HeadersMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/Interceptors/InterceptorsMacro.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor APIMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/API/APIMacro.swift
  </files>
  <action>
Read current APIMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template for API class/struct generation
3. Use Template.variableDeclaration() for properties

The @API macro generates API client class/struct scaffolding. Pattern:
```swift
import MacroTemplateKit

// For property generation
let baseURLTemplate: Template<Void> = .variableDeclaration(
  name: "baseURL",
  type: "URL",
  initializer: .function("URL", arguments: [
    (label: "string", value: .literal(config.baseURL))
  ])
)

// For method generation
let methodTemplate: Template<Void> = .function(
  "async",
  arguments: [
    (label: "throws", value: .literal(true))
  ]
)
```

CRITICAL: Preserve exact expansion output.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter APIMacro 2>&1 | tail -10
```
  </verify>
  <done>
APIMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor BodyMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/BodyMacro.swift
  </files>
  <action>
Read current BodyMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template for body encoding generation

Pattern for body encoding:
```swift
let bodyTemplate: Template<Void> = .function(
  "encoder.encode",
  arguments: [
    (label: nil, value: .variable(bodyParameterName, payload: ()))
  ]
)
```

CRITICAL: Preserve exact expansion output.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter BodyMacro 2>&1 | tail -10
```
  </verify>
  <done>
BodyMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor HeadersMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/HeadersMacro.swift
  </files>
  <action>
Read current HeadersMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template.arrayLiteral() for header collection
3. Use Template for individual header generation

Pattern:
```swift
let headersTemplate: Template<Void> = .arrayLiteral(
  headers.map { header in
    .function("setValue", arguments: [
      (label: nil, value: .variable(header.valueParam, payload: ())),
      (label: "forHTTPHeaderField", value: .literal(header.name))
    ])
  }
)
```

CRITICAL: Preserve exact expansion output.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter HeadersMacro 2>&1 | tail -10
```
  </verify>
  <done>
HeadersMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Refactor InterceptorsMacro.swift and InterceptorCodeGenerator.swift</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/Interceptors/InterceptorsMacro.swift
    Packages/NetworkingMacros/Sources/NetworkingMacros/Interceptors/InterceptorCodeGenerator.swift
  </files>
  <action>
Read current Interceptors implementation, then refactor both files:

**InterceptorsMacro.swift:**
- Replace raw SwiftSyntax construction with Template builders
- Use Template for interceptor chain generation

**InterceptorCodeGenerator.swift:**
- Replace code generation logic with Template composition
- Use Template.function() for interceptor instantiation
- Use Template.arrayLiteral() for interceptor lists

Pattern:
```swift
let chainTemplate: Template<Void> = .function(
  "InterceptorChain",
  arguments: [
    (label: "interceptors", value: .arrayLiteral(
      interceptors.map { .variable($0.name, payload: ()) }
    ))
  ]
)
```

CRITICAL: Preserve exact expansion output for both files.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter Interceptors 2>&1 | tail -10
```
  </verify>
  <done>
InterceptorsMacro.swift and InterceptorCodeGenerator.swift refactored to use Template algebra, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Final verification - all 13 macros using Template algebra</name>
  <files></files>
  <action>
Verify all macros are refactored:

1. Count macros importing MacroTemplateKit (should be 13):
```bash
rg "import MacroTemplateKit" Packages/NetworkingMacros/Sources/NetworkingMacros/ | wc -l
```

2. Verify no raw SwiftSyntax construction in expansion methods:
```bash
rg "IntegerLiteralExprSyntax\(|StringLiteralExprSyntax\(|FunctionCallExprSyntax\(" \
  Packages/NetworkingMacros/Sources/NetworkingMacros/**/*Macro.swift
```

3. Run full test suite:
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test
```

4. Build entire workspace:
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking && swift build -Xswiftc -warnings-as-errors
```
  </action>
  <verify>
```bash
# Count MacroTemplateKit imports (expect 13+ files)
rg -l "import MacroTemplateKit" \
  Packages/NetworkingMacros/Sources/NetworkingMacros/ | wc -l

# Full test suite
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift test 2>&1 | grep -E "passed|failed"

# Workspace build
cd /Users/bruno/Developer/Inbox/ModernNetworking && \
swift build -Xswiftc -warnings-as-errors
```
  </verify>
  <done>
All 13 macros verified using Template algebra, full test suite passes, workspace builds successfully.
  </done>
</task>

</tasks>

<verification>
```bash
# Comprehensive verification

# 1. All macros import MacroTemplateKit
rg "import MacroTemplateKit" Packages/NetworkingMacros/Sources/NetworkingMacros/

# 2. No raw SwiftSyntax in macro files (should be empty)
rg "IntegerLiteralExprSyntax\(|StringLiteralExprSyntax\(" \
  Packages/NetworkingMacros/Sources/NetworkingMacros/**/*Macro.swift

# 3. Full NetworkingMacros test suite
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && swift test

# 4. Full workspace build
cd /Users/bruno/Developer/Inbox/ModernNetworking && \
swift build -Xswiftc -warnings-as-errors

# 5. Full workspace tests
cd /Users/bruno/Developer/Inbox/ModernNetworking && swift test
```
</verification>

<success_criteria>
1. APIMacro.swift refactored to use Template algebra
2. BodyMacro.swift refactored to use Template algebra
3. HeadersMacro.swift refactored to use Template algebra
4. InterceptorsMacro.swift and InterceptorCodeGenerator.swift refactored
5. All 13 macros import and use MacroTemplateKit
6. Zero raw SwiftSyntax node construction in macro expansion methods
7. All macro tests pass with identical expansion output
8. All packages build with `-Xswiftc -warnings-as-errors`
9. TMPL-12 requirement satisfied
10. Phase 10 COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/10-refactor-networkingmacros-to-functional-template-render-api/10-08-SUMMARY.md`
</output>
