---
phase: 10-refactor-networkingmacros-to-functional-template-render-api
plan: 07
type: execute
wave: 7
depends_on: ["10-06"]
files_modified:
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/CacheableMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/MeasuredMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/TimeoutMacro.swift
  - Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/DefaultHeadersMacro.swift
autonomous: true

must_haves:
  truths:
    - "Configuration macros use Template algebra instead of raw SwiftSyntax"
    - "Zero raw SwiftSyntax node construction in configuration macro expansion"
    - "All configuration macro tests pass with identical expansion output"
    - "All packages build with -Xswiftc -warnings-as-errors"
  artifacts:
    - path: "Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/CacheableMacro.swift"
      provides: "Refactored @Cacheable macro using Template algebra"
      contains: "import MacroTemplateKit"
    - path: "Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/MeasuredMacro.swift"
      provides: "Refactored @Measured macro using Template algebra"
      contains: "import MacroTemplateKit"
  key_links:
    - from: "Configuration/*Macro.swift"
      to: "MacroTemplateKit"
      via: "Template construction for code generation"
      pattern: "Template<"
---

<objective>
Refactor configuration macros (Cacheable, Measured, Timeout, DefaultHeaders) to use MacroTemplateKit's Template algebra.

Purpose: Replace manual SwiftSyntax AST construction with type-safe, composable templates.
Output: 4 refactored configuration macro files using Template algebra.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-refactor-networkingmacros-to-functional-template-render-api/10-06-SUMMARY.md

# Current configuration macro implementations to refactor
@Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/CacheableMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/MeasuredMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/TimeoutMacro.swift
@Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/DefaultHeadersMacro.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CacheableMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/CacheableMacro.swift
  </files>
  <action>
Read current CacheableMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template.function() for interceptor configuration
3. Use Template.literal() for configuration values

Pattern:
```swift
import MacroTemplateKit
import SwiftSyntax
import SwiftSyntaxMacros

public struct CacheableMacro: PeerMacro {
  public static func expansion(...) throws -> [DeclSyntax] {
    // Extract cache configuration from macro arguments
    let config = extractCacheConfig(from: node)

    // Build cache interceptor configuration using Template
    let interceptorTemplate: Template<Void> = .function(
      "CachingInterceptor",
      arguments: [
        (label: "policy", value: .variable(config.policy, payload: ())),
        (label: "duration", value: .literal(config.duration))
      ]
    )

    // Render and build declaration
    let interceptorExpr = Renderer.render(interceptorTemplate)
    return [buildInterceptorDeclaration(interceptorExpr)]
  }
}
```

CRITICAL: Preserve exact expansion output - behavior must not change.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter CacheableMacro 2>&1 | tail -10
```
  </verify>
  <done>
CacheableMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor MeasuredMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/MeasuredMacro.swift
  </files>
  <action>
Read current MeasuredMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template for metrics interceptor configuration
3. Handle optional metric name parameter

Pattern:
```swift
let metricsTemplate: Template<Void> = .function(
  "MetricsInterceptor",
  arguments: [
    (label: "name", value: config.name.map { .literal($0) } ?? .literal(functionName))
  ]
)
```

CRITICAL: Preserve exact expansion output.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter MeasuredMacro 2>&1 | tail -10
```
  </verify>
  <done>
MeasuredMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor TimeoutMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/TimeoutMacro.swift
  </files>
  <action>
Read current TimeoutMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template.literal() for timeout duration
3. Use Template.function() for timeout configuration

Pattern:
```swift
let timeoutTemplate: Template<Void> = .function(
  "withTimeout",
  arguments: [
    (label: "seconds", value: .literal(config.seconds))
  ]
)
```

CRITICAL: Preserve exact expansion output.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter TimeoutMacro 2>&1 | tail -10
```
  </verify>
  <done>
TimeoutMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Refactor DefaultHeadersMacro.swift to use Template algebra</name>
  <files>
    Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/DefaultHeadersMacro.swift
  </files>
  <action>
Read current DefaultHeadersMacro.swift implementation, then refactor to use Template algebra:

1. Replace raw SwiftSyntax construction with Template builders
2. Use Template.arrayLiteral() for header list
3. Use Template.function() for header construction

Pattern:
```swift
let headersTemplate: Template<Void> = .arrayLiteral(
  headers.map { header in
    .function("HTTPHeader", arguments: [
      (label: "name", value: .literal(header.name)),
      (label: "value", value: .literal(header.value))
    ])
  }
)
```

CRITICAL: Preserve exact expansion output.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors && \
swift test --filter DefaultHeadersMacro 2>&1 | tail -10
```
  </verify>
  <done>
DefaultHeadersMacro.swift refactored to use Template algebra, imports MacroTemplateKit, all tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
# Build NetworkingMacros package
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift build -Xswiftc -warnings-as-errors

# Run all configuration macro tests
cd /Users/bruno/Developer/Inbox/ModernNetworking/Packages/NetworkingMacros && \
swift test --filter "Cacheable|Measured|Timeout|DefaultHeaders"

# Verify MacroTemplateKit import in all configuration macros
rg "import MacroTemplateKit" \
  Packages/NetworkingMacros/Sources/NetworkingMacros/Configuration/*.swift

# Build workspace
cd /Users/bruno/Developer/Inbox/ModernNetworking && \
swift build -Xswiftc -warnings-as-errors
```
</verification>

<success_criteria>
1. All 4 configuration macros import MacroTemplateKit
2. Zero raw SwiftSyntax node construction in configuration macro expansion
3. All configuration macro tests pass with identical expansion output
4. All packages build with `-Xswiftc -warnings-as-errors`
5. TMPL-11 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-refactor-networkingmacros-to-functional-template-render-api/10-07-SUMMARY.md`
</output>
