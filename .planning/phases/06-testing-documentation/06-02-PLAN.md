---
phase: 06-testing-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/NetworkingTests/BDD/NetworkClientBehaviorSpec.swift
  - Tests/NetworkingTests/BDD/InterceptorChainBehaviorSpec.swift
  - Tests/NetworkingTests/BDD/ErrorHandlingBehaviorSpec.swift
autonomous: true

must_haves:
  truths:
    - "BDD specs describe user-facing behaviors in natural language"
    - "BDD specs cover NetworkClient request execution"
    - "BDD specs cover interceptor chain processing"
    - "BDD specs cover error handling and recovery"
    - "New BDD specs supplement existing SimpleBDDTests.swift"
  artifacts:
    - path: "Tests/NetworkingTests/BDD/NetworkClientBehaviorSpec.swift"
      provides: "BDD specs for NetworkClient behaviors"
      min_lines: 80
    - path: "Tests/NetworkingTests/BDD/InterceptorChainBehaviorSpec.swift"
      provides: "BDD specs for interceptor chain"
      min_lines: 60
    - path: "Tests/NetworkingTests/BDD/ErrorHandlingBehaviorSpec.swift"
      provides: "BDD specs for error handling"
      min_lines: 60
  key_links:
    - from: "BDD specs"
      to: "Quick/Nimble"
      via: "framework imports"
      pattern: "import Quick.*import Nimble"
    - from: "BDD specs"
      to: "SimpleBDDTests"
      via: "supplements existing BDD coverage"
      pattern: "Tests/NetworkingTests/SimpleBDDTests.swift"
---

<objective>
Expand BDD specs for user-facing behaviors (TEST-06) and audit existing integration tests (TEST-07).

Purpose: BDD specs document expected behaviors in readable, business-oriented language. The existing SimpleBDDTests.swift covers basic type behaviors but lacks coverage of NetworkClient execution flows, interceptor processing, and error recovery scenarios. New specs supplement (not replace) existing coverage.

Output: 3 new BDD spec files covering core user behaviors + documented audit of existing integration tests.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Tests/NetworkingTests/SimpleBDDTests.swift
@Tests/NetworkingTests/BatchProgressIntegrationTests.swift
@Tests/NetworkingTests/DSL/ResponseChainingIntegrationTests.swift
@Tests/NetworkingTests/Interceptors/InterceptorIntegrationTests.swift
@Sources/Networking/Testing/MockNetworkClient.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NetworkClientBehaviorSpec</name>
  <files>Tests/NetworkingTests/BDD/NetworkClientBehaviorSpec.swift</files>
  <action>
Create BDD directory and NetworkClientBehaviorSpec.swift:

```bash
mkdir -p Tests/NetworkingTests/BDD
```

Create spec with Quick/Nimble following existing SimpleBDDTests pattern:

```swift
import Quick
import Nimble
import XCTest
@testable import Networking
import Foundation

final class NetworkClientBehaviorSpec: QuickSpec {
  override class func spec() {
    describe("NetworkClient") {
      describe("executing HTTP requests") {
        context("when the request succeeds") {
          it("returns the response with correct status") { ... }
          it("decodes JSON response bodies") { ... }
        }

        context("when the server returns an error") {
          it("throws HTTPError with correct status code") { ... }
          it("includes response body in error for debugging") { ... }
        }

        context("when the network is unavailable") {
          it("throws network connection error") { ... }
        }
      }

      describe("using request middleware") {
        context("when authentication is configured") {
          it("adds Authorization header to requests") { ... }
        }

        context("when multiple middleware are configured") {
          it("processes middleware in correct order") { ... }
        }
      }

      describe("caching responses") {
        context("when cache is enabled") {
          it("returns cached response for identical requests") { ... }
          it("respects cache TTL") { ... }
        }
      }
    }
  }
}
```

Use MockNetworkClient for stubbing. Follow async test patterns from SimpleBDDTests. Each `it` block should have Given/When/Then structure in comments.

**NOTE**: This supplements existing SimpleBDDTests.swift (which covers basic type behaviors). These new specs focus on NetworkClient execution flows.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking && swift test --filter NetworkClientBehaviorSpec 2>&1 | tail -20
```
All NetworkClient BDD specs pass.
  </verify>
  <done>NetworkClientBehaviorSpec exists with 8+ BDD specs covering request execution, middleware, and caching.</done>
</task>

<task type="auto">
  <name>Task 2: Create InterceptorChainBehaviorSpec and ErrorHandlingBehaviorSpec</name>
  <files>
Tests/NetworkingTests/BDD/InterceptorChainBehaviorSpec.swift
Tests/NetworkingTests/BDD/ErrorHandlingBehaviorSpec.swift
  </files>
  <action>
Create two additional BDD spec files:

**InterceptorChainBehaviorSpec.swift:**
```swift
describe("InterceptorChain") {
  describe("processing requests") {
    context("with request interceptors") {
      it("transforms request before sending") { ... }
      it("allows interceptor to abort chain") { ... }
    }

    context("with response interceptors") {
      it("transforms response after receiving") { ... }
    }

    context("when composing chains") {
      it("appends interceptors in correct order") { ... }
      it("empty chain passes request through unchanged") { ... }
    }
  }
}
```

**ErrorHandlingBehaviorSpec.swift:**
```swift
describe("Error Handling") {
  describe("HTTP errors") {
    context("4xx client errors") {
      it("classifies 400 as bad request") { ... }
      it("classifies 401 as unauthorized") { ... }
      it("classifies 404 as not found") { ... }
    }

    context("5xx server errors") {
      it("classifies 500 as internal server error") { ... }
      it("classifies 503 as service unavailable") { ... }
    }
  }

  describe("recovery strategies") {
    context("when error is retryable") {
      it("suggests retry for timeout errors") { ... }
      it("suggests retry for 503 errors") { ... }
    }

    context("when error is not retryable") {
      it("does not suggest retry for 401") { ... }
      it("does not suggest retry for validation errors") { ... }
    }
  }
}
```

Each spec should test observable behaviors, NOT implementation details. Use HTTPError and HTTPStatus from existing types.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking && swift test --filter "BehaviorSpec" 2>&1 | tail -30
```
All BDD specs pass (InterceptorChain, ErrorHandling, NetworkClient).
  </verify>
  <done>InterceptorChainBehaviorSpec and ErrorHandlingBehaviorSpec exist with combined 12+ specs.</done>
</task>

<task type="auto">
  <name>Task 3: Audit existing integration tests and document coverage</name>
  <files>Tests/NetworkingTests/BDD/INTEGRATION_TEST_AUDIT.md</files>
  <action>
Audit existing integration tests and document coverage. The actual integration test files are:

- `Tests/NetworkingTests/BatchProgressIntegrationTests.swift`
- `Tests/NetworkingTests/DSL/ResponseChainingIntegrationTests.swift`
- `Tests/NetworkingTests/Interceptors/InterceptorIntegrationTests.swift`

**NOTE**: There is NO `IntegrationTests.swift` file - integration tests are distributed across multiple files.

1. **Read and audit each integration test file**, documenting coverage:
   - BatchProgressIntegrationTests: Batch operations, progress tracking
   - ResponseChainingIntegrationTests: DSL response chaining
   - InterceptorIntegrationTests: Interceptor chain integration

2. **Create INTEGRATION_TEST_AUDIT.md** in BDD/ directory with:
   ```markdown
   # Integration Test Coverage Audit

   ## Existing Integration Test Files

   ### BatchProgressIntegrationTests.swift
   - Location: Tests/NetworkingTests/BatchProgressIntegrationTests.swift
   - Coverage: [list what's tested]
   - Test count: N tests

   ### ResponseChainingIntegrationTests.swift
   - Location: Tests/NetworkingTests/DSL/ResponseChainingIntegrationTests.swift
   - Coverage: [list what's tested]
   - Test count: N tests

   ### InterceptorIntegrationTests.swift
   - Location: Tests/NetworkingTests/Interceptors/InterceptorIntegrationTests.swift
   - Coverage: [list what's tested]
   - Test count: N tests

   ## Coverage Summary
   - Total integration tests: N
   - Areas covered: [list]
   - Potential gaps: [list if any]

   ## Recommendations
   - [Any suggested additions if gaps found]
   ```

3. **If gaps identified**, note them in audit but do NOT create new test files (scope limitation).

This task documents existing coverage rather than adding new integration tests.
  </action>
  <verify>
```bash
cd /Users/bruno/Developer/Inbox/ModernNetworking && swift test --filter "IntegrationTests" 2>&1 | tail -20
```
All existing integration tests pass.
  </verify>
  <done>Integration test audit complete, coverage documented in INTEGRATION_TEST_AUDIT.md.</done>
</task>

</tasks>

<verification>
1. Build verification:
   ```bash
   cd /Users/bruno/Developer/Inbox/ModernNetworking && swift build -Xswiftc -warnings-as-errors
   ```

2. BDD test verification:
   ```bash
   cd /Users/bruno/Developer/Inbox/ModernNetworking && swift test --filter "BehaviorSpec|BDD"
   ```

3. Integration test verification:
   ```bash
   cd /Users/bruno/Developer/Inbox/ModernNetworking && swift test --filter "IntegrationTests"
   ```

4. Requirement verification:
   - TEST-06 (BDD specs): 3 BDD spec files with user-facing behavior coverage
   - TEST-07 (Integration tests): Existing integration tests audited and documented
</verification>

<success_criteria>
1. BDD/ directory exists under Tests/NetworkingTests/
2. NetworkClientBehaviorSpec.swift exists with 8+ specs
3. InterceptorChainBehaviorSpec.swift exists with 5+ specs
4. ErrorHandlingBehaviorSpec.swift exists with 6+ specs
5. INTEGRATION_TEST_AUDIT.md documents existing integration test coverage
6. All BDD specs pass with `swift test`
7. All existing integration tests pass with `swift test`
8. Build passes with `-Xswiftc -warnings-as-errors`
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-documentation/06-02-SUMMARY.md`
</output>
