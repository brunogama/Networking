# Plans

Store task-specific execution plans here when `current-plan.yaml` is too small.

For this repo, the canonical long-form architecture plan is:

- `docs/architecture-refactoring-execution-plan.md`

Files in this directory should distill that source into active checkpoints, current blockers, and
ordered next actions.
